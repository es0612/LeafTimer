# Issue #59 + #60: A11y/i18n バンドル Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** TimerView の開始/停止を VoiceOver 操作可能な本物の Button にし (#59)、設定画面のハードコード英語文言 17 箇所を NSLocalizedString 化する (#60)。

**Architecture:** i18n キーを先に TDD (キー検知テスト RED → strings 追加 GREEN) で固め、その上で #60 の文言置換 → #59 の Button 化を各々 RED→GREEN で実装。仕上げは full `make tests` + Simulator トップ画面の ja/en × light/dark 回帰。

**Tech Stack:** SwiftUI / NSLocalizedString + Localizable.strings (ja/en .lproj) / XCTest + Quick/Nimble + ViewInspector / xcodeproj gem (`app/bin/add-to-target.rb`)

**Spec:** `docs/superpowers/specs/2026-08-03-a11y-bundle-design.md` (承認済み)

## Global Constraints

- 作業ブランチ: `feature/59-60-a11y-i18n` (作成済み・spec commit 済み)。default branch は **`master`**。
- ビルド/テスト系コマンドは exit code 直接判定。パイプフィルタ必要時は `set -o pipefail` 前置 (shell は zsh、`${PIPESTATUS[0]}` は使わない)。成功マーカー (`** TEST SUCCEEDED **`) / 失敗マーカーの両方で判定してもよい。
- xcodebuild 系 Bash は timeout 600000 (10 分) を明示。
- 新規 Swift ファイルは `app/bin/add-to-target.rb` で target attach → `make sort` → `make precheck` で orphan でないことを確認。
- Bash でのパス組み立ては絶対パス (`/Users/shinya/workspace/claude/LeafTimer/...`)。
- commit メッセージ末尾に Co-Authored-By: Claude Fable 5 <noreply@anthropic.com> と Claude-Session: https://claude.ai/code/session_01AhWe83sr1w4mwbDfrjuABd のトレーラーを付ける。
- キー命名: 設定画面 = `settings.<name>` / `settings.<section>.<name>`、共通 = `common.<name>`、a11y = `timer.a11y.<name>`。
- Swift の配列リテラルに trailing comma を書かない (プロジェクトは Swift 5 系)。

---

### Task 1: SettingsLocalizationTests 新設 (RED)

**Files:**
- Create: `app/LeafTimerTests/SettingsLocalizationTests.swift`
- Modify: `app/LeafTimer.xcodeproj/project.pbxproj` (add-to-target.rb 経由)

**Interfaces:**
- Consumes: 既存 `StatLocalizationTests` のパターン (`Bundle(for: TimerViewModel.self)` からロケール別 lproj を解決)
- Produces: 新キー 24 個の一覧 (このファイルの `newKeys` 配列が Task 2 の strings 追加、Task 3/5 のコード置換で使うキーの正)

- [ ] **Step 1: テストファイルを作成**

`app/LeafTimerTests/SettingsLocalizationTests.swift`:

```swift
// app/LeafTimerTests/SettingsLocalizationTests.swift
import XCTest
@testable import LeafTimer

/// Issue #59/#60 で追加したキーが ja/en 両方の Localizable.strings に存在することを検証する。
/// パターンは StatLocalizationTests を踏襲 (テスト実行環境の言語設定に依存しない)。
final class SettingsLocalizationTests: XCTestCase {

    private static let newKeys = [
        // #60: 設定画面
        "settings.done",
        "settings.mode_footer",
        "settings.timer.preview_button",
        "settings.timer.preview_title",
        "settings.timer.preview_work",
        "settings.timer.preview_break",
        "settings.sound_footer",
        "settings.reset.button",
        "settings.reset.alert_title",
        "settings.reset.confirm",
        "settings.reset.alert_message",
        "settings.reset.done_title",
        "settings.reset.done_message",
        "settings.system_section",
        "settings.footer.app_name",
        "settings.footer.copyright",
        "common.cancel",
        "common.ok",
        // #59: VoiceOver ラベル
        "timer.a11y.start",
        "timer.a11y.stop",
        "timer.a11y.reset",
        "timer.a11y.history",
        "timer.a11y.settings",
        "timer.a11y.remaining_time"
    ]

    /// 指定ロケールの .lproj から key を解決する（simulator の言語設定に依存しない）。
    private func localized(_ key: String, locale: String) -> String {
        let appBundle = Bundle(for: TimerViewModel.self)
        guard let path = appBundle.path(forResource: locale, ofType: "lproj"),
              let lproj = Bundle(path: path) else {
            return "<<missing \(locale).lproj>>"
        }
        return lproj.localizedString(forKey: key, value: "<<missing>>", table: nil)
    }

    func testAllNewKeysExistInJapanese() {
        for key in Self.newKeys {
            XCTAssertNotEqual(localized(key, locale: "ja"), "<<missing>>", "ja.lproj に \(key) が無い")
        }
    }

    func testAllNewKeysExistInEnglish() {
        for key in Self.newKeys {
            XCTAssertNotEqual(localized(key, locale: "en"), "<<missing>>", "en.lproj に \(key) が無い")
        }
    }

    func testRepresentativeJapaneseValues() {
        XCTAssertEqual(localized("settings.done", locale: "ja"), "完了")
        XCTAssertEqual(localized("settings.system_section", locale: "ja"), "システム")
        XCTAssertEqual(localized("timer.a11y.start", locale: "ja"), "タイマーを開始")
    }

    func testRepresentativeEnglishValues() {
        XCTAssertEqual(localized("settings.done", locale: "en"), "Done")
        XCTAssertEqual(localized("timer.a11y.stop", locale: "en"), "Stop timer")
    }
}
```

- [ ] **Step 2: target attach + sort + precheck**

```bash
cd /Users/shinya/workspace/claude/LeafTimer/app
ruby bin/add-to-target.rb LeafTimer.xcodeproj LeafTimerTests/SettingsLocalizationTests.swift LeafTimerTests LeafTimerTests
make sort
make precheck
```

Expected: precheck が pass (新規 orphan なし)。

- [ ] **Step 3: RED を確認 (このテストクラスのみ実行)**

```bash
cd /Users/shinya/workspace/claude/LeafTimer/app && xcodebuild -workspace LeafTimer.xcworkspace -scheme LeafTimer -destination "platform=iOS Simulator,name=iPhone 17,OS=latest" test -only-testing:LeafTimerTests/SettingsLocalizationTests
```

(timeout 600000)
Expected: **FAIL**。失敗値の予測: `localized(...)` が `"<<missing>>"` を返し、`testAllNewKeysExistInJapanese` / `...English` / 代表値テストが assert 失敗 (キー未登録のため)。ビルドエラーではなく **テスト失敗**であることを確認する。

### Task 2: Localizable.strings に 24 キー追加 (GREEN)

**Files:**
- Modify: `app/LeafTimer/App/en.lproj/Localizable.strings` (末尾に追記)
- Modify: `app/LeafTimer/App/ja.lproj/Localizable.strings` (末尾に追記)

**Interfaces:**
- Consumes: Task 1 の `newKeys` 一覧
- Produces: 全 24 キーの ja/en 値 (Task 3/5 のコードが `NSLocalizedString` で参照)

- [ ] **Step 1: en.lproj に追記**

`app/LeafTimer/App/en.lproj/Localizable.strings` の末尾に追加:

```text
// MARK: - Common (Issue #60)
"common.cancel" = "Cancel";
"common.ok" = "OK";

// MARK: - Settings View hardcoded copy (Issue #60)
"settings.done" = "Done";
"settings.mode_footer" = "Pomodoro technique helps you stay focused with regular breaks.";
"settings.timer.preview_button" = "Preview Timer Settings";
"settings.timer.preview_title" = "Timer Preview";
"settings.timer.preview_work" = "Work Session";
"settings.timer.preview_break" = "Break Time";
"settings.sound_footer" = "Tap the play button to preview sounds.";
"settings.reset.button" = "Reset to Default Settings";
"settings.reset.alert_title" = "Reset Settings";
"settings.reset.confirm" = "Reset";
"settings.reset.alert_message" = "This will reset all settings to their default values. This action cannot be undone.";
"settings.reset.done_title" = "Settings Reset";
"settings.reset.done_message" = "All settings have been reset to defaults.";
"settings.system_section" = "System";
"settings.footer.app_name" = "LeafTimer - Focus & Productivity";
"settings.footer.copyright" = "© 2025 LeafTimer. All rights reserved.";

// MARK: - Timer View accessibility (Issue #59)
"timer.a11y.start" = "Start timer";
"timer.a11y.stop" = "Stop timer";
"timer.a11y.reset" = "Reset timer";
"timer.a11y.history" = "History";
"timer.a11y.settings" = "Settings";
"timer.a11y.remaining_time" = "Remaining time";
```

- [ ] **Step 2: ja.lproj に追記**

`app/LeafTimer/App/ja.lproj/Localizable.strings` の末尾に追加:

```text
// MARK: - Common (Issue #60)
"common.cancel" = "キャンセル";
"common.ok" = "OK";

// MARK: - Settings View hardcoded copy (Issue #60)
"settings.done" = "完了";
"settings.mode_footer" = "ポモドーロ・テクニックは定期的な休憩で集中力を保ちます。";
"settings.timer.preview_button" = "タイマー設定をプレビュー";
"settings.timer.preview_title" = "タイマープレビュー";
"settings.timer.preview_work" = "作業セッション";
"settings.timer.preview_break" = "休憩時間";
"settings.sound_footer" = "再生ボタンをタップするとサウンドを試聴できます。";
"settings.reset.button" = "設定を初期値に戻す";
"settings.reset.alert_title" = "設定のリセット";
"settings.reset.confirm" = "リセット";
"settings.reset.alert_message" = "すべての設定が初期値に戻ります。この操作は取り消せません。";
"settings.reset.done_title" = "リセット完了";
"settings.reset.done_message" = "すべての設定を初期値に戻しました。";
"settings.system_section" = "システム";
"settings.footer.app_name" = "LeafTimer - Focus & Productivity";
"settings.footer.copyright" = "© 2025 LeafTimer. All rights reserved.";

// MARK: - Timer View accessibility (Issue #59)
"timer.a11y.start" = "タイマーを開始";
"timer.a11y.stop" = "タイマーを停止";
"timer.a11y.reset" = "タイマーをリセット";
"timer.a11y.history" = "履歴";
"timer.a11y.settings" = "設定";
"timer.a11y.remaining_time" = "残り時間";
```

- [ ] **Step 3: GREEN を確認**

Task 1 Step 3 と同じコマンドを実行。
Expected: **PASS** (`** TEST SUCCEEDED **`)。

- [ ] **Step 4: Commit**

```bash
git add app/LeafTimerTests/SettingsLocalizationTests.swift app/LeafTimer.xcodeproj/project.pbxproj app/LeafTimer/App/en.lproj/Localizable.strings app/LeafTimer/App/ja.lproj/Localizable.strings
git commit -m "test: #59 #60 の新規 Localizable キー 24 個とキー検知テストを追加"
```

### Task 3: #60 設定画面の文言置換

**Files:**
- Modify: `app/LeafTimer/View/EnhancedSettingView.swift:43,85`
- Modify: `app/LeafTimer/View/Settings/TimerSettingsSection.swift:86,122,128,138,148`
- Modify: `app/LeafTimer/View/Settings/SoundSettingsSection.swift:96`
- Modify: `app/LeafTimer/View/Settings/ResetSettingsSection.swift:18,27,28,29,33,35,36,38,45,51,53`
- Modify: `app/LeafTimerTests/ModernSettingViewSpec.swift` (代表文言の配線テスト追加)

**Interfaces:**
- Consumes: Task 2 のキー (`settings.*` / `common.*`)
- Produces: 設定画面からハードコード英語文言ゼロ

- [ ] **Step 1: 代表文言の配線テストを追加 (RED)**

`app/LeafTimerTests/ModernSettingViewSpec.swift` の最上位 `describe` ブロック内の末尾に追加 (既存の `beforeEach` 変数には依存しない自己完結ブロック):

```swift
            describe("Localized copy (Issue #60)") {
                it("renders the reset button label from Localizable.strings") {
                    let viewModel = SettingViewModel(userDefaultWrapper: LocalUserDefaultsWrapper())
                    let view = EnhancedSettingView(settingViewModel: viewModel)
                    expect {
                        try view.inspect().find(text: NSLocalizedString("settings.reset.button", comment: ""))
                    }.toNot(throwError())
                }
            }
```

- [ ] **Step 2: RED を確認**

```bash
cd /Users/shinya/workspace/claude/LeafTimer/app && xcodebuild -workspace LeafTimer.xcworkspace -scheme LeafTimer -destination "platform=iOS Simulator,name=iPhone 17,OS=latest" test -only-testing:LeafTimerTests/ModernSettingViewSpec
```

(timeout 600000)
Expected: **FAIL**。失敗値の予測: 画面上の Text はまだハードコード `"Reset to Default Settings"` のままだが、`NSLocalizedString("settings.reset.button")` は Task 2 で追加済みの値を返す — en 実行環境では両者が同値になり **素通り (vacuously green) しうる**。素通りした場合はこのテストの検索文字列を `NSLocalizedString` ではなく ja 値 `"設定を初期値に戻す"` 固定にせず、**実行環境が en であることを前提に en 値と同値になる事実を記録し、Step 4 の置換後も PASS が維持されることを回帰として扱う** (このテストの主目的は置換後の配線検証)。

- [ ] **Step 3: 文言を置換 (17 箇所)**

各ファイルの該当行を以下のとおり置換する:

`EnhancedSettingView.swift`:
- :43 `Text("Pomodoro technique helps you stay focused with regular breaks.")` → `Text(NSLocalizedString("settings.mode_footer", comment: "Mode section footer"))`
- :85 `Button("Done") {` → `Button(NSLocalizedString("settings.done", comment: "Done button")) {`

`TimerSettingsSection.swift`:
- :86 `Text("Preview Timer Settings")` → `Text(NSLocalizedString("settings.timer.preview_button", comment: "Preview timer settings button"))`
- :122 `Text("Timer Preview")` → `Text(NSLocalizedString("settings.timer.preview_title", comment: "Timer preview sheet title"))`
- :128 `title: "Work Session",` → `title: NSLocalizedString("settings.timer.preview_work", comment: "Work session preview label"),`
- :138 `title: "Break Time",` → `title: NSLocalizedString("settings.timer.preview_break", comment: "Break time preview label"),`
- :148 `Button("Done") { dismiss() }` → `Button(NSLocalizedString("settings.done", comment: "Done button")) { dismiss() }`

`SoundSettingsSection.swift`:
- :96 `Text("Tap the play button to preview sounds.")` → `Text(NSLocalizedString("settings.sound_footer", comment: "Sound section footer"))`

`ResetSettingsSection.swift`:
- :18 `Text("Reset to Default Settings")` → `Text(NSLocalizedString("settings.reset.button", comment: "Reset settings button"))`
- :27 `.alert("Reset Settings", isPresented:` → `.alert(NSLocalizedString("settings.reset.alert_title", comment: "Reset alert title"), isPresented:`
- :28 `Button("Cancel", role: .cancel)` → `Button(NSLocalizedString("common.cancel", comment: "Cancel"), role: .cancel)`
- :29 `Button("Reset", role: .destructive)` → `Button(NSLocalizedString("settings.reset.confirm", comment: "Reset confirm"), role: .destructive)`
- :33 `Text("This will reset all settings to their default values. This action cannot be undone.")` → `Text(NSLocalizedString("settings.reset.alert_message", comment: "Reset alert message"))`
- :35 `.confirmationDialog("Settings Reset", isPresented:` → `.confirmationDialog(NSLocalizedString("settings.reset.done_title", comment: "Reset done title"), isPresented:`
- :36 `Button("OK")` → `Button(NSLocalizedString("common.ok", comment: "OK"))`
- :38 `Text("All settings have been reset to defaults.")` → `Text(NSLocalizedString("settings.reset.done_message", comment: "Reset done message"))`
- :45 `Text("System")` → `Text(NSLocalizedString("settings.system_section", comment: "System section header"))`
- :51 `Text("LeafTimer - Focus & Productivity")` → `Text(NSLocalizedString("settings.footer.app_name", comment: "Footer app name"))`
- :53 `Text("© 2025 LeafTimer. All rights reserved.")` → `Text(NSLocalizedString("settings.footer.copyright", comment: "Footer copyright"))`

(行番号は 2026-08-03 時点。ズレていたら文字列で探す)

- [ ] **Step 4: キー整合 grep + テスト PASS を確認**

```bash
cd /Users/shinya/workspace/claude/LeafTimer
for f in app/LeafTimer/View/EnhancedSettingView.swift app/LeafTimer/View/Settings/*.swift app/LeafTimer/View/TimerView.swift; do grep -o 'NSLocalizedString("[^"]*"' "$f"; done | sed 's/NSLocalizedString("//;s/"$//' | sort -u | while read -r k; do
  grep -q "\"$k\"" app/LeafTimer/App/en.lproj/Localizable.strings || echo "MISSING en: $k"
  grep -q "\"$k\"" app/LeafTimer/App/ja.lproj/Localizable.strings || echo "MISSING ja: $k"
done
```

Expected: `MISSING` 出力ゼロ (コード中の全キーが両 strings に存在)。
続けて Task 3 Step 2 と同じ `-only-testing:LeafTimerTests/ModernSettingViewSpec` を実行し **PASS** を確認。

- [ ] **Step 5: Commit**

```bash
git add app/LeafTimer/View/EnhancedSettingView.swift app/LeafTimer/View/Settings/TimerSettingsSection.swift app/LeafTimer/View/Settings/SoundSettingsSection.swift app/LeafTimer/View/Settings/ResetSettingsSection.swift app/LeafTimerTests/ModernSettingViewSpec.swift
git commit -m "fix(i18n): #60 設定画面のハードコード英語文言 17 箇所を NSLocalizedString 化"
```

### Task 4: #59 Button 化の RED テスト

**Files:**
- Modify: `app/LeafTimerTests/ModernTimerViewSpec.swift` (describe 追加)

**Interfaces:**
- Consumes: 既存 `ModernTimerViewSpec` の `beforeEach` 変数 (`timerView: TimerView!`)
- Produces: 「開始/停止は CircleButton を包む本物の Button」の構造検証テスト

- [ ] **Step 1: テストを追加**

`app/LeafTimerTests/ModernTimerViewSpec.swift` の最上位 `describe("Modernized TimerView")` ブロック内の末尾に追加:

```swift
            describe("Accessibility (Issue #59)") {
                it("start/stop control is a real Button wrapping CircleButton") {
                    expect {
                        try timerView.body.inspect().find(ViewType.Button.self, where: { candidate in
                            (try? candidate.find(CircleButton.self)) != nil
                        })
                    }.toNot(throwError())
                }
            }
```

- [ ] **Step 2: RED を確認**

```bash
cd /Users/shinya/workspace/claude/LeafTimer/app && xcodebuild -workspace LeafTimer.xcworkspace -scheme LeafTimer -destination "platform=iOS Simulator,name=iPhone 17,OS=latest" test -only-testing:LeafTimerTests/ModernTimerViewSpec
```

(timeout 600000)
Expected: **FAIL**。失敗値の予測: 現状の CircleButton は `.onTapGesture` 付き View で Button が存在しないため、`find` が `ViewSearchError` を throw して `toNot(throwError())` が失敗する。

### Task 5: #59 TimerView の Button 化 + a11y ラベル (GREEN)

**Files:**
- Modify: `app/LeafTimer/View/TimerView.swift:51-64,89-110`

**Interfaces:**
- Consumes: `TimerViewModel.executeState: Bool` (@Published、実行中 = true) / `getDisplayedTime() -> String` / Task 2 の `timer.a11y.*` キー
- Produces: VoiceOver 操作可能な開始/停止 Button + ラベル付きツールバー + 残り時間の accessibilityValue

- [ ] **Step 1: 開始/停止を Button 化**

`TimerView.swift:60-64` の

```swift
                    CircleButton(viewModel: timerViewModel)
                        .shadow(color: .gray, radius: 1, x: 1, y: 2)
                        .onTapGesture {
                            didTapTimerButton()
                        }
```

を以下に置換:

```swift
                    Button(action: didTapTimerButton) {
                        CircleButton(viewModel: timerViewModel)
                            .shadow(color: .gray, radius: 1, x: 1, y: 2)
                    }
                    .buttonStyle(.plain)
                    .accessibilityLabel(
                        timerViewModel.executeState
                            ? NSLocalizedString("timer.a11y.stop", comment: "Stop timer button")
                            : NSLocalizedString("timer.a11y.start", comment: "Start timer button")
                    )
```

- [ ] **Step 2: 残り時間に accessibilityLabel/Value を付与**

`TimerView.swift:51-58` の `Text(timerViewModel.getDisplayedTime())` のモディファイア末尾 (`.padding(.bottom, 50)` の直後) に追加:

```swift
                        .accessibilityLabel(NSLocalizedString("timer.a11y.remaining_time", comment: "Remaining time label"))
                        .accessibilityValue(timerViewModel.getDisplayedTime())
```

- [ ] **Step 3: ツールバー 3 アイコンにラベルを付与**

- リセット (`:91-93` の `Button(action: didTapResetButton) { ... }`) に `.accessibilityLabel(NSLocalizedString("timer.a11y.reset", comment: "Reset timer button"))`
- 履歴 (`:96-104` の `NavigationLink(destination: HistoryView(...)) { ... }`) に `.accessibilityLabel(NSLocalizedString("timer.a11y.history", comment: "History button"))`
- 設定 (`:106-108` の `NavigationLink(destination: EnhancedSettingView(...)) { ... }`) に `.accessibilityLabel(NSLocalizedString("timer.a11y.settings", comment: "Settings button"))`

いずれも各 View の closing brace 直後にモディファイアとして追加する。

- [ ] **Step 4: GREEN を確認**

Task 4 Step 2 と同じコマンドを実行。
Expected: **PASS** (`** TEST SUCCEEDED **`、既存テストも含め全 PASS)。

- [ ] **Step 5: Commit**

```bash
git add app/LeafTimer/View/TimerView.swift app/LeafTimerTests/ModernTimerViewSpec.swift
git commit -m "fix(a11y): #59 開始/停止を Button 化し VoiceOver ラベルを付与"
```

(注: ModernTimerViewSpec.swift は Task 4 で変更済みだが未 commit のためここで一緒に commit)

### Task 6: 総合検証 + PR + マージ

**Files:** なし (検証と git/gh 操作)

**Interfaces:**
- Consumes: Task 1-5 の全変更
- Produces: CI green な PR → master マージ、Issue #59/#60 クローズ

- [ ] **Step 1: full make tests**

```bash
cd /Users/shinya/workspace/claude/LeafTimer/app && make tests
```

(timeout 600000)
Expected: `** TEST SUCCEEDED **` を含み exit 0。

- [ ] **Step 2: Simulator でトップ画面の ja/en × light/dark 回帰スクショ**

REQUIRED SUB-SKILL: `ios-simulator-locale-testing` (および `ios-simulator-app-verification`) に従う。要点:
- ビルド成果物は `find app/build` で探さず、`xcodebuild ... -showBuildSettings | grep -m1 BUILT_PRODUCTS_DIR` で取得した `$BUILT_DIR/LeafTimer.app` を使う (CLAUDE.md ルール)
- onboarding は UserDefaults キー (キー名は `SettingViewModel.shouldShowOnboarding()` の実装から読む) を pre-set してバイパスする
- `-AppleLanguages '(ja)'` / `'(en)'` × `xcrun simctl ui <SIM> appearance light|dark` の 4 枚を撮る
- スクショは `SendUserFile` でユーザーに渡す (PR 本文にローカルパスを書かない — CLAUDE.md ルール)

Expected: 4 枚とも従来と同じ見た目 (今回の変更は見た目に影響しないことの回帰確認)。

- [ ] **Step 3: push + PR 作成**

```bash
git fetch && gh pr list --state all --head feature/59-60-a11y-i18n
git push -u origin feature/59-60-a11y-i18n
gh pr create --base master --title "fix: VoiceOver 対応 (#59) + 設定画面の i18n (#60)" --body "$(cat <<'EOF'
## 概要
- #59: 開始/停止を本物の Button 化 (VoiceOver / Switch Control 対応)、ツールバー 3 アイコンと残り時間に a11y ラベル付与
- #60: 設定画面のハードコード英語文言 17 箇所を NSLocalizedString 化 (ja/en キー 24 個追加)

- 設計: docs/superpowers/specs/2026-08-03-a11y-bundle-design.md
- 実装計画: docs/superpowers/plans/2026-08-03-a11y-bundle.md
- テスト: SettingsLocalizationTests (キー検知) / ModernSettingViewSpec (文言配線) / ModernTimerViewSpec (Button 構造)

## スクリーンショット
(ここにトップ画面 ja/en × light/dark の 4 枚をドラッグ&ドロップで添付してください)

## 検証
- [x] TDD: キー検知テスト RED→GREEN / Button 構造テスト RED→GREEN
- [x] full make tests PASS
- [x] Simulator 回帰スクショ 4 枚 (ユーザーに送付済み)

Closes #59
Closes #60

🤖 Generated with [Claude Code](https://claude.com/claude-code)

https://claude.ai/code/session_01AhWe83sr1w4mwbDfrjuABd
EOF
)"
```

- [ ] **Step 4: CI green 確認**

```bash
gh pr checks --watch --interval 30
```

(timeout 600000、タイムアウト時は再実行して継続)
Expected: `pr-tests` pass (今 session で必須チェック化済み)。red なら `gh run view <ID> --log-failed` で原因を読み修正、3 回で直らなければ BLOCKED 報告。

- [ ] **Step 5: マージ**

REQUIRED SUB-SKILL: `superpowers:finishing-a-development-branch` に従う (マージ方式は merge commit: `gh pr merge --merge`)。マージ後:

```bash
git checkout master && git pull --ff-only
gh issue view 59 --json state --jq .state
gh issue view 60 --json state --jq .state
```

Expected: 両方 `CLOSED`。

---

## Self-Review 済みチェック

- spec カバレッジ: Button 化 (Task 5) / ツールバー・残り時間ラベル (Task 5) / 17 箇所置換 (Task 3) / キー検知テスト (Task 1-2) / 配線検証 + grep (Task 3) / Simulator 回帰 (Task 6) — 全て対応 Task あり
- Task 3 Step 2 の RED は en 環境で vacuously green になりうることを明記済み (主目的は置換後の配線回帰)
- 型整合: `executeState: Bool` (@Published 確認済み) / `getDisplayedTime() -> String` / キー名は Task 1 の `newKeys` と Task 2 の strings と Task 3/5 の置換で一致
