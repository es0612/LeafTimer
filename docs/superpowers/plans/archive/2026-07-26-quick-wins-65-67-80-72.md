# 小粒 quick win まとめ (#80 / #65 / #67 / #72) Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 互いに独立した4件の小粒 issue — #80 (ビルド設定の一元化) / #65 (ダミー UI の露出) / #67 (LaunchScreen ダークフラッシュ) / #72 (本番デッドコード) — を1本の PR で解消する。

**Architecture:** 4タスクは相互に依存しない。ただし **#80 だけがテストハーネス自体 (`make precheck`) に触る**ため最初に置く。ここが壊れると後続3タスクのテスト結果が全て信用できなくなるので、最も早く・単独で検出できる位置に配置する。以降は影響範囲の小さい順に #65 (単一ブロック削除) → #67 (asset + storyboard) → #72 (テスト編集 + 2ファイル削除) とする。

**Tech Stack:** SwiftUI / Xcode 15+ / iOS 17+ / xcodebuild + iPhone 17 Simulator / Quick + Nimble + ViewInspector / Ruby (minitest) / fastlane / CocoaPods

## Global Constraints

- **作業ディレクトリ:** 全ての `make` / `xcodebuild` コマンドは `/Users/shinya/workspace/claude/LeafTimer/app` で実行する。**`simctl` に渡すバンドルパスは必ず絶対パスで書く** (`cd app` 後の相対パスは `app/app/...` に二重化して2分タイムアウトで無言失敗する — Issue #57 の実績)。
- **テスト実行のタイムアウト:** Bash から `make unit-tests` / `make tests` を呼ぶ時は `timeout` を `600000` (10分) に設定する。simulator boot + build + test で2〜5分かかる。
- **成否判定:** この shell は **zsh**。`${PIPESTATUS[0]}` は空を返す (bash 専用構文) ので使わない。パイプを使うなら `set -o pipefail` を前置し、加えて出力中の `** TEST SUCCEEDED **` / `** BUILD SUCCEEDED **` / `** TEST FAILED **` / `Error 6x` マーカーで判定する。
- **作業ファイルの置き場所:** ビルドログ・スクリーンショット・一時バックアップは全て `/private/tmp/claude-501/-Users-shinya-workspace-claude-LeafTimer/aaa7ab20-a7de-42c1-9088-8ece7c643b3c/scratchpad/` 配下に置く (システムの `/tmp` は使わない)。ディレクトリは作成済み。
- **ビルド成果物 (`.app`) の場所を `find` で探さない。** 🚨 このリポジトリには古い `app/build/DerivedData/Build/Products/Debug-iphonesimulator/LeafTimer.app` (2026-07-18 の残骸) が残っており、`find app/build` はこれを掴む。`Makefile` の `xcodebuild` は `-derivedDataPath` を指定していないので、**実際の成果物はデフォルトの `~/Library/Developer/Xcode/DerivedData/LeafTimer-<hash>/Build/Products/Debug-iphonesimulator/` に出る**。8日前のアプリを install して「変更が反映されていない」と誤診しないよう、パスは必ず `xcodebuild -showBuildSettings` の `BUILT_PRODUCTS_DIR` から取得する (各 Simulator 検証ステップに記載のコマンドを使う)。
- **`project.pbxproj` を編集したら** 最終 commit の前に `make sort` を実行し、`git status` で差分が残っていないことを確認する。
- **ブランチ:** `feature/quick-wins-65-67-80-72`。この plan ファイル自体をブランチ作成直後の**最初の commit** にする (PR 化後の追い commit にしない)。
- **既存 PR 確認:** push 前に `git fetch && gh pr list --state all --head feature/quick-wins-65-67-80-72` で空振りを防ぐ。
- **触らないもの:** `docs/superpowers/plans/*` と `plans/*` の過去 plan/spec に出てくる `LeafTimer Release` / `iPhone 17` は**当時の記録**なので修正対象外。修正するのは実行される設定 (`Makefile` / `Fastfile`) と現行ドキュメント (`docs/ver1_2/xcode-cloud-setup.md`) のみ。
- **スコープ注記 (見積もり変更):** #80 は当初 XS 見積もりだったが、`app/bin/xcode_precheck.rb` が Makefile の `-destination` 行を**テキストとしてパースしている**依存が判明したため **S に拡大**した。素朴に変数化すると `destination_name` が `$(SIMULATOR)` という文字列を simulator 名として返し、`make precheck` (= `make tests` の前段) が「シミュレータ不在」で RED になる。Task 1 はこの解決を含む。

---

### Task 0: ブランチ作成と plan のコミット

**Files:**
- Create: `docs/superpowers/plans/2026-07-26-quick-wins-65-67-80-72.md` (このファイル)

- [ ] **Step 1: master が最新であることを確認**

```bash
cd /Users/shinya/workspace/claude/LeafTimer && git fetch origin && git status --short --branch
```

Expected: `## master...origin/master` のみ (ahead/behind の表示が無い)。CLAUDE.md への振り返り追記が uncommitted で残っている場合はそれも含めて次の commit に入れる。

- [ ] **Step 2: ブランチを作成**

```bash
cd /Users/shinya/workspace/claude/LeafTimer && git checkout -b feature/quick-wins-65-67-80-72
```

注意: このリポジトリの default branch は **`master`** (`main` ではない)。

- [ ] **Step 3: plan と CLAUDE.md の振り返り追記を commit**

```bash
cd /Users/shinya/workspace/claude/LeafTimer && \
git add docs/superpowers/plans/2026-07-26-quick-wins-65-67-80-72.md CLAUDE.md && \
git commit -m "docs(plan): #80 #65 #67 #72 小粒 quick win まとめの実装計画

前セッションの振り返り2件 (simctl 絶対パス / subagent session limit 時の再 dispatch) の
CLAUDE.md 反映も同梱する。"
```

---

### Task 1: #80 — シミュレータ名の一元化と Fastfile の scheme 修正

**Files:**
- Modify: `app/bin/xcode_precheck.rb` — `destination_name` に Make 変数解決を追加
- Modify: `app/bin/test_xcode_precheck.rb` — 変数解決のテストを追加
- Modify: `app/Makefile:8-13` — `SIMULATOR` 変数を導入
- Modify: `app/fastlane/Fastfile:10,26` — device を Makefile 由来にする / scheme を実在名に修正
- Modify: `docs/ver1_2/xcode-cloud-setup.md:50` — scheme を実在名に修正

**Interfaces:**
- Produces: `XcodePrecheck.destination_name(makefile_text) -> String | nil` — 既存シグネチャを維持したまま、`$(VAR)` 形式を同じ Makefile 内の `VAR ?= value` / `VAR = value` で解決するようになる。変数に代入が無い場合は `$(VAR)` の生文字列をそのまま返す (呼び出し側が「そんなシミュレータは無い」と大声で失敗できるように)。
- Produces: `XcodePrecheck.expand_variable(var_name, makefile_text) -> String | nil` — 新規 public メソッド。
- Consumes (Task 1 内): `app/fastlane/Fastfile` が `require_relative '../bin/xcode_precheck'` でこのモジュールを利用する。

#### 背景 (実装者向け)

`app/bin/` は **2層構成**で、これは意図的なもの:
- `xcode_precheck.rb` (アンダースコア) = 純粋ロジック module、I/O なし、minitest 対象
- `xcode-precheck.rb` (ハイフン) = CLI glue、ファイル読み込みや `xcrun` 実行を担当

新しいロジックは**必ず純粋ロジック層 (`xcode_precheck.rb`) に置く**。CLI 層に書くとテストできない。

- [ ] **Step 1: 失敗するテストを書く**

`app/bin/test_xcode_precheck.rb` の `destination_name` 関連テスト群の直後 (既存の `test_destination_name_*` の後ろ) に追加:

```ruby
  def test_destination_name_resolves_make_variable
    makefile = <<~MAKE
      SIMULATOR ?= iPhone 17

      unit-tests:
      \t-destination "platform=iOS Simulator,name=$(SIMULATOR),OS=latest" \\
    MAKE
    assert_equal 'iPhone 17', XcodePrecheck.destination_name(makefile)
  end

  def test_destination_name_resolves_plain_assignment
    makefile = <<~MAKE
      SIMULATOR = iPhone SE (3rd generation)

      unit-tests:
      \t-destination "platform=iOS Simulator,name=$(SIMULATOR),OS=latest" \\
    MAKE
    assert_equal 'iPhone SE (3rd generation)', XcodePrecheck.destination_name(makefile)
  end

  def test_destination_name_keeps_unassigned_variable_visible
    # 代入が無い変数は解決できない。生の "$(SIMULATOR)" を返して
    # 呼び出し側 (precheck) が「そんなシミュレータは無い」と失敗できるようにする。
    makefile = <<~MAKE
      unit-tests:
      \t-destination "platform=iOS Simulator,name=$(SIMULATOR),OS=latest" \\
    MAKE
    assert_equal '$(SIMULATOR)', XcodePrecheck.destination_name(makefile)
  end

  def test_expand_variable_returns_nil_when_absent
    assert_nil XcodePrecheck.expand_variable('SIMULATOR', "all:\n\techo hi\n")
  end
```

- [ ] **Step 2: テストを実行して失敗を確認する**

```bash
cd /Users/shinya/workspace/claude/LeafTimer/app && ruby bin/test_xcode_precheck.rb
```

Expected: **4件中2件が FAIL、2件が PASS**。予測される失敗値を必ず突き合わせること:

| テスト | 期待 | 現状の実際値 | 判定 |
| --- | --- | --- | --- |
| `test_destination_name_resolves_make_variable` | `"iPhone 17"` | `"$(SIMULATOR)"` | **FAIL** |
| `test_destination_name_resolves_plain_assignment` | `"iPhone SE (3rd generation)"` | `"$(SIMULATOR)"` | **FAIL** |
| `test_destination_name_keeps_unassigned_variable_visible` | `"$(SIMULATOR)"` | `"$(SIMULATOR)"` | PASS (regression guard) |
| `test_expand_variable_returns_nil_when_absent` | `nil` | `NoMethodError` (未定義) | **FAIL/ERROR** |

⚠️ 3件目は**未実装でも green** になる。これはフォールバック挙動を意図的に温存しているためで、vacuously green ではなく「現状挙動を固定する regression guard」。1件目・2件目が RED になっていることを目視確認してから次へ進むこと。もし1件目が PASS していたら、テストが実装を通っていない (typo 等) ので先に調査する。

- [ ] **Step 3: 最小の実装を書く**

`app/bin/xcode_precheck.rb` の `destination_name` を差し替え、直後に `expand_variable` を追加:

```ruby
  # Extract the simulator name from an xcodebuild `-destination` spec in a
  # Makefile (or any text). When the name is a Make variable reference such
  # as `$(SIMULATOR)`, resolve it against that same Makefile's `VAR ?= value`
  # or `VAR = value` assignment (Issue #80: the name is declared once at the
  # top of the Makefile and consumed by both `make unit-tests` and fastlane).
  # Returns nil when no destination is present, and the unresolved `$(VAR)`
  # text when the variable has no assignment — so the caller still fails
  # loudly instead of silently checking the wrong simulator.
  def self.destination_name(makefile_text)
    m = makefile_text.match(/-destination\s+"[^"]*?name=([^,"]+)/)
    return nil unless m

    name = m[1].strip
    var = name[/\A\$[({]([A-Za-z_][A-Za-z0-9_]*)[)}]\z/, 1]
    return name unless var

    expand_variable(var, makefile_text) || name
  end

  # Resolve a Make variable assignment (`VAR = value`, `VAR ?= value`,
  # `VAR := value`) from Makefile text. Returns nil when unassigned.
  def self.expand_variable(var, makefile_text)
    m = makefile_text.match(/^#{Regexp.escape(var)}\s*[:?+]?=\s*(.+?)\s*$/)
    m && m[1].strip
  end
```

- [ ] **Step 4: テストを実行して通ることを確認する**

```bash
cd /Users/shinya/workspace/claude/LeafTimer/app && ruby bin/test_xcode_precheck.rb
```

Expected: `0 failures, 0 errors` (既存テストを含めて全 PASS)。

- [ ] **Step 5: Makefile に `SIMULATOR` 変数を導入する**

`app/Makefile` の先頭コメント直後に変数を追加し、`unit-tests` の destination を差し替える。変更前:

```make
# Pods/ is intentionally not committed and may be absent locally.
# Run `make install` to fetch dependencies before the first build.
install:
	pod install
update:
	pod update

unit-tests:
	@/usr/bin/time xcodebuild \
	-workspace 'LeafTimer.xcworkspace' \
	-scheme "LeafTimer" \
	-destination "platform=iOS Simulator,name=iPhone 17,OS=latest" \
	build test
```

変更後:

```make
# Pods/ is intentionally not committed and may be absent locally.
# Run `make install` to fetch dependencies before the first build.

# Simulator used by `make unit-tests`, validated by `make precheck`, and read
# back by fastlane (app/fastlane/Fastfile). Single source of truth — Issue #80.
# Xcode を更新して機種が消えたら、この1行だけを直す。
SIMULATOR ?= iPhone 17

install:
	pod install
update:
	pod update

unit-tests:
	@/usr/bin/time xcodebuild \
	-workspace 'LeafTimer.xcworkspace' \
	-scheme "LeafTimer" \
	-destination "platform=iOS Simulator,name=$(SIMULATOR),OS=latest" \
	build test
```

- [ ] **Step 6: precheck が実際に変数を解決できることを確認する (GREEN 側)**

```bash
cd /Users/shinya/workspace/claude/LeafTimer/app && ruby bin/xcode-precheck.rb
```

Expected: シミュレータ不在のエラーが出ないこと。`$(SIMULATOR)` という文字列がエラーメッセージに出たら Step 3 の実装が効いていない。

- [ ] **Step 7: 意図的に壊して precheck が RED になることを確認する (FAIL 側)**

⚠️ **このステップを飛ばさないこと。** checker の最大の盲点は「正常系が green なだけ」の vacuous green。壊れた入力で本当に RED になるかを実証する。

```bash
cd /Users/shinya/workspace/claude/LeafTimer/app && \
cp Makefile /private/tmp/claude-501/-Users-shinya-workspace-claude-LeafTimer/aaa7ab20-a7de-42c1-9088-8ece7c643b3c/scratchpad/Makefile.quickwin-backup && \
sed -i '' 's/^SIMULATOR ?= iPhone 17$/SIMULATOR ?= iPhone 999 (does not exist)/' Makefile && \
ruby bin/xcode-precheck.rb; echo "exit=$?"
```

Expected: **非ゼロ exit** と「シミュレータが見つからない」旨のエラー。`iPhone 999 (does not exist)` という**解決後の名前**がメッセージに出ること (`$(SIMULATOR)` のままなら解決が効いていない)。

復元:

```bash
cd /Users/shinya/workspace/claude/LeafTimer/app && \
cp /private/tmp/claude-501/-Users-shinya-workspace-claude-LeafTimer/aaa7ab20-a7de-42c1-9088-8ece7c643b3c/scratchpad/Makefile.quickwin-backup Makefile && \
git diff --stat Makefile && ruby bin/xcode-precheck.rb; echo "exit=$?"
```

Expected: `git diff --stat` に Makefile の変更 (SIMULATOR 導入分) が残り、precheck は exit 0。

- [ ] **Step 8: Fastfile を Makefile 由来に変更し、scheme を修正する**

`app/fastlane/Fastfile` の冒頭 (`update_fastlane` の前) に追加:

```ruby
require_relative '../bin/xcode_precheck'

# シミュレータ名は app/Makefile の SIMULATOR 変数を単一の情報源にする (Issue #80)。
# fastlane の作業ディレクトリは app/fastlane/ なので、__dir__ 起点の絶対パスで
# 親ディレクトリの Makefile を読む (相対パスだと実行位置によって壊れる)。
SIMULATOR_NAME = XcodePrecheck.destination_name(
  File.read(File.expand_path('../Makefile', __dir__))
).freeze

update_fastlane
```

`lane :unittests` の `device:` を差し替え:

```ruby
  lane :unittests do
  scan(
    device: SIMULATOR_NAME,
    clean: true,
    scheme: "LeafTimer"
  )
  end
```

`lane :beta` の `build_app` の scheme を実在名に修正 (`LeafTimer Release` という scheme は存在しない。共有 scheme は `LeafTimer` の1本のみ):

```ruby
    build_app(workspace: "LeafTimer.xcworkspace", scheme: "LeafTimer")
```

- [ ] **Step 9: Fastfile が構文的に正しく、Makefile を読めることを確認する**

fastlane 本体を起動せずに、require と読み取りだけを検証する:

```bash
cd /Users/shinya/workspace/claude/LeafTimer/app/fastlane && \
ruby -e "require_relative '../bin/xcode_precheck'; \
puts XcodePrecheck.destination_name(File.read(File.expand_path('../Makefile', __dir__)))"
```

Expected: `iPhone 17` と出力される (`$(SIMULATOR)` ではなく)。

続けて Fastfile 自体の構文チェック:

```bash
cd /Users/shinya/workspace/claude/LeafTimer/app && ruby -c fastlane/Fastfile
```

Expected: `Syntax OK`

- [ ] **Step 10: ドキュメントの scheme 名を修正する**

`docs/ver1_2/xcode-cloud-setup.md:50` の該当行を修正。変更前:

```
    - Scheme: `LeafTimer Release` (既存 scheme、`Fastfile` の `build_app` でも使用)
```

変更後:

```
    - Scheme: `LeafTimer` (共有 scheme はこの1本のみ。`Fastfile` の `build_app` でも使用)
```

- [ ] **Step 11: 残存を確認する**

**この Task で変更したファイルだけ**を対象にする。リポジトリ全体を grep すると、履歴として残す過去 plan/spec (`docs/superpowers/plans/`, `docs/superpowers/specs/`, `plans/`) がヒットして「出力ゼロ」にならない:

```bash
cd /Users/shinya/workspace/claude/LeafTimer && \
grep -n "LeafTimer Release" app/fastlane/Fastfile docs/ver1_2/xcode-cloud-setup.md
```

Expected: **出力ゼロ** (grep の exit code は 1 になるが、これは「見つからない」= 正常)。

参考: リポジトリ全体には `docs/superpowers/specs/2026-05-23-xcode-cloud-migration-design.md:34,47` などに `LeafTimer Release` が残るが、これらは**当時の記録**なので修正対象外 (Global Constraints 参照)。

- [ ] **Step 12: commit**

```bash
cd /Users/shinya/workspace/claude/LeafTimer && \
git add app/Makefile app/bin/xcode_precheck.rb app/bin/test_xcode_precheck.rb \
        app/fastlane/Fastfile docs/ver1_2/xcode-cloud-setup.md && \
git commit -m "fix(build): #80 シミュレータ名を Makefile に一元化し Fastfile の scheme を実在名に修正

- Makefile に SIMULATOR 変数を導入し、Fastfile が Makefile から読むようにした
- xcode_precheck.destination_name が \$(VAR) を Makefile 内の代入で解決するよう拡張
  (変数化により precheck が simulator 不在と誤判定するのを防ぐ)
- 存在しない \"LeafTimer Release\" scheme 参照を Fastfile と xcode-cloud-setup.md で修正"
```

---

### Task 2: #65 — 常時 disabled の Volume ダミースライダーを削除する

**Files:**
- Modify: `app/LeafTimer/View/Settings/SoundSettingsSection.swift` (Volume ブロックと footer 文言)
- Modify: `app/LeafTimerTests/ModernSettingViewSpec.swift` (対応する空テストの削除)

**Interfaces:**
- Consumes: なし (Task 1 とは独立)
- Produces: なし (View の内部ブロック削除のみ。公開シグネチャは変わらない)

#### 背景 (実装者向け)

`Slider(value: .constant(0.5))` を `.disabled(true)` + `.opacity(0.5)` で置いた "Future Enhancement Placeholder" が本番の設定画面にそのまま出ている。表示は常に "50%" 固定で、ユーザーには**壊れたコントロール**に見える。ボリューム機能そのものの実装は別 issue の判断事項なので、ここでは**露出を止めるだけ**にする。

- [ ] **Step 1: Volume ブロックを削除する**

`app/LeafTimer/View/Settings/SoundSettingsSection.swift` から次のブロックを**丸ごと削除**する:

```swift
            // Sound Volume Control (Future Enhancement Placeholder)
            VStack(alignment: .leading, spacing: 8) {
                HStack {
                    Label("Volume", systemImage: "speaker.wave.3.fill")
                        .font(.system(size: 15, weight: .medium))
                        .foregroundColor(.primary)

                    Spacer()

                    Text("50%")
                        .font(.system(size: 14))
                        .foregroundColor(.secondary)
                }

                Slider(value: .constant(0.5), in: 0...1)
                    .tint(.blue)
                    .disabled(true) // Placeholder for future implementation
                    .opacity(0.5)
            }
            .padding(.vertical, 4)
```

削除後、直前の `.padding(.vertical, 4)` で終わるブロックと `} header: {` の間に余分な空行が2つ以上残らないよう整えること。

- [ ] **Step 2: 宙に浮く footer 文言を修正する**

同ファイルの footer にある文言を修正する。変更前:

```swift
            Text("Tap the play button to preview sounds. Volume control coming in future update.")
```

変更後 (削除した機能への言及を落とすだけ。ローカライズ対応は #60 の範囲なので、ここでは英語のまま短縮する):

```swift
            Text("Tap the play button to preview sounds.")
```

- [ ] **Step 3: 対応する空テストを削除する**

`app/LeafTimerTests/ModernSettingViewSpec.swift` から次の `it` ブロックを削除する (中身が `expect(true) == true` の何も検証していないテスト):

```swift
                it("includes volume control slider") {
                    // Future enhancement: volume control
                    expect(true) == true
                }
```

- [ ] **Step 4: ビルドとテストを実行する**

```bash
cd /Users/shinya/workspace/claude/LeafTimer/app && set -o pipefail && \
make unit-tests 2>&1 | tee /private/tmp/claude-501/-Users-shinya-workspace-claude-LeafTimer/aaa7ab20-a7de-42c1-9088-8ece7c643b3c/scratchpad/quickwin-task2.log | tail -30
```

(Bash tool の `timeout` は `600000` を指定すること)

Expected: 出力に `** TEST SUCCEEDED **` が含まれる。`** TEST FAILED **` や `Error 6x` が出ていないことを次で確認する:

```bash
grep -c "\*\* TEST SUCCEEDED \*\*" /private/tmp/claude-501/-Users-shinya-workspace-claude-LeafTimer/aaa7ab20-a7de-42c1-9088-8ece7c643b3c/scratchpad/quickwin-task2.log; \
grep -c "\*\* TEST FAILED \*\*\|Error 6" /private/tmp/claude-501/-Users-shinya-workspace-claude-LeafTimer/aaa7ab20-a7de-42c1-9088-8ece7c643b3c/scratchpad/quickwin-task2.log
```

Expected: 1行目 `1` 以上、2行目 `0`。

- [ ] **Step 5: Simulator で目視確認する**

設定画面から Volume 行が消えていることを確認する。**バンドルパスは必ず絶対パスで書く**こと:

```bash
xcrun simctl boot "iPhone 17" 2>/dev/null || true
```

ビルド成果物のパスを特定してからインストール:

```bash
cd /Users/shinya/workspace/claude/LeafTimer/app && \
BUILT_DIR=$(xcodebuild -workspace LeafTimer.xcworkspace -scheme LeafTimer -destination "platform=iOS Simulator,name=iPhone 17,OS=latest" -showBuildSettings 2>/dev/null | grep -m1 "BUILT_PRODUCTS_DIR" | sed 's/.*= //'); \
APP_PATH="$BUILT_DIR/LeafTimer.app"; \
echo "APP_PATH=$APP_PATH"; \
xcrun simctl install "iPhone 17" "$APP_PATH" && \
xcrun simctl launch "iPhone 17" com.shinya.LeafTimer
```

bundle id が異なる場合は `/usr/libexec/PlistBuddy -c "Print CFBundleIdentifier" "$APP_PATH/Info.plist"` で確認する。

設定画面はタップ操作が必要だが `simctl` に `tap` は無い。スクリーンショットで確認できる範囲 (起動画面) を撮り、設定画面の目視は Task 4 完了後の最終確認にまとめてよい:

```bash
xcrun simctl io "iPhone 17" screenshot /private/tmp/claude-501/-Users-shinya-workspace-claude-LeafTimer/aaa7ab20-a7de-42c1-9088-8ece7c643b3c/scratchpad/quickwin-task2-launch.png
```

- [ ] **Step 6: commit**

```bash
cd /Users/shinya/workspace/claude/LeafTimer && \
git add app/LeafTimer/View/Settings/SoundSettingsSection.swift app/LeafTimerTests/ModernSettingViewSpec.swift && \
git commit -m "fix(settings): #65 常時 disabled の Volume ダミースライダーを本番 UI から削除

表示が \"50%\" 固定で操作もできず、ユーザーには壊れたコントロールに見えていた。
機能実装まで露出させない。宙に浮く footer 文言と、何も検証していない空テストも併せて削除。"
```

---

### Task 3: #67 — LaunchScreen をライト/ダーク両対応にする

**Files:**
- Create: `app/LeafTimer/App/Assets.xcassets/LaunchBackground.colorset/Contents.json`
- Modify: `app/LeafTimer/App/Base.lproj/LaunchScreen.storyboard`

**Interfaces:**
- Consumes: なし
- Produces: named color `LaunchBackground` (asset 名。storyboard から参照される)

#### 背景 (実装者向け) — 重要な前提2つ

**(1) `appearance="light"` は犯人ではない。** storyboard 3行目の `<device id="retina6_1" orientation="portrait" appearance="light"/>` は **Interface Builder のキャンバス表示設定**で、実行時の挙動には影響しない。実際にダークモードでフラッシュを起こしているのは `<color key="backgroundColor" ... displayP3 淡緑 />` の**固定色**の方。ただし issue が指摘しており紛らわしいので、`appearance` 属性も併せて外す (cosmetic)。

**(2) 新しい colorset は `App/Assets.xcassets` に置くこと。** このリポジトリにはディスク上に asset catalog が2つあるが、**`project.pbxproj` に登録されているのは `app/LeafTimer/App/Assets.xcassets` の1つだけ**。もう一方の `app/LeafTimer/Assets.xcassets` (`Colors/` に14個の colorset を含む) は**どの target にも attach されておらず、ビルドに一切含まれない** (これが Issue #63 の「未使用の14個のセマンティックカラー資産」の正体)。未登録カタログに色を作ると、起動時に色の解決に失敗して**フラッシュどころか黒画面**になる。

確認コマンド (実装前に自分の目で確かめること):

```bash
cd /Users/shinya/workspace/claude/LeafTimer/app && \
grep -n "Assets.xcassets" LeafTimer.xcodeproj/project.pbxproj
```

Expected: `Assets.xcassets` の `PBXFileReference` が **1件だけ**存在し、それが `App` グループ (`path = App;`) の children に入っていること。

- [ ] **Step 1: named color を作成する**

`app/LeafTimer/App/Assets.xcassets/LaunchBackground.colorset/Contents.json` を新規作成する。light 値は現行 storyboard の背景色をそのまま、dark 値は `TimerViewModel+extensions.swift` の work × dark グラデーション上端 `(0.18, 0.22, 0.18)` に合わせる (起動直後に表示されるトップ画面と地続きに見せるため):

```json
{
  "colors" : [
    {
      "color" : {
        "color-space" : "display-p3",
        "components" : {
          "alpha" : "1.000",
          "blue" : "0.847",
          "green" : "0.911",
          "red" : "0.847"
        }
      },
      "idiom" : "universal"
    },
    {
      "appearances" : [
        {
          "appearance" : "luminosity",
          "value" : "dark"
        }
      ],
      "color" : {
        "color-space" : "display-p3",
        "components" : {
          "alpha" : "1.000",
          "blue" : "0.180",
          "green" : "0.220",
          "red" : "0.180"
        }
      },
      "idiom" : "universal"
    }
  ],
  "info" : {
    "author" : "xcode",
    "version" : 1
  }
}
```

注意: asset catalog はフォルダ参照として1つの `PBXFileReference` にまとまっているため、**colorset を足しても `project.pbxproj` の編集は不要**。

- [ ] **Step 2: storyboard を named color 参照に差し替える**

`app/LeafTimer/App/Base.lproj/LaunchScreen.storyboard` を3箇所編集する。

(a) `<device>` 行から `appearance` 属性を外す。変更前:

```xml
    <device id="retina6_1" orientation="portrait" appearance="light"/>
```

変更後:

```xml
    <device id="retina6_1" orientation="portrait"/>
```

(b) 背景色を named color 参照にする。変更前:

```xml
                        <color key="backgroundColor" red="0.84708183999999997" green="0.91117113829999996" blue="0.8470515609" alpha="1" colorSpace="custom" customColorSpace="displayP3"/>
```

変更後:

```xml
                        <color key="backgroundColor" name="LaunchBackground"/>
```

(c) `<resources>` に namedColor を宣言する。変更前:

```xml
    <resources>
        <image name="splashIcon" width="300" height="179"/>
    </resources>
```

変更後:

```xml
    <resources>
        <image name="splashIcon" width="300" height="179"/>
        <namedColor name="LaunchBackground">
            <color red="0.84708183999999997" green="0.91117113829999996" blue="0.8470515609" alpha="1" colorSpace="custom" customColorSpace="displayP3"/>
        </namedColor>
    </resources>
```

(`<resources>` 内の色は Interface Builder のプレビュー用フォールバック。実行時は asset catalog の値が使われる。)

- [ ] **Step 3: ビルドが通ることを確認する**

```bash
cd /Users/shinya/workspace/claude/LeafTimer/app && set -o pipefail && \
xcodebuild -workspace LeafTimer.xcworkspace -scheme LeafTimer \
  -destination "platform=iOS Simulator,name=iPhone 17,OS=latest" \
  build 2>&1 | tee /private/tmp/claude-501/-Users-shinya-workspace-claude-LeafTimer/aaa7ab20-a7de-42c1-9088-8ece7c643b3c/scratchpad/quickwin-task3-build.log | tail -20
```

(Bash tool の `timeout` は `600000` を指定すること)

Expected: 出力に `** BUILD SUCCEEDED **`。storyboard のコンパイルエラー (`ibtool`) が出た場合は Step 2 の XML を見直す。

- [ ] **Step 4: ダークモードで LaunchScreen を目視検証する**

⚠️ **LaunchScreen はシステムがキャッシュする。** 再起動 (`terminate` → `launch`) だけでは古い LaunchScreen が出るので、**必ず uninstall → install** すること。バンドルパスは絶対パスで組む。

```bash
xcrun simctl boot "iPhone 17" 2>/dev/null || true; \
xcrun simctl ui "iPhone 17" appearance dark; \
cd /Users/shinya/workspace/claude/LeafTimer/app && \
BUILT_DIR=$(xcodebuild -workspace LeafTimer.xcworkspace -scheme LeafTimer -destination "platform=iOS Simulator,name=iPhone 17,OS=latest" -showBuildSettings 2>/dev/null | grep -m1 "BUILT_PRODUCTS_DIR" | sed 's/.*= //'); \
APP_PATH="$BUILT_DIR/LeafTimer.app"; \
BUNDLE_ID=$(/usr/libexec/PlistBuddy -c "Print CFBundleIdentifier" "$APP_PATH/Info.plist"); \
echo "APP_PATH=$APP_PATH BUNDLE_ID=$BUNDLE_ID"; \
xcrun simctl uninstall "iPhone 17" "$BUNDLE_ID"; \
xcrun simctl install "iPhone 17" "$APP_PATH"; \
xcrun simctl launch "iPhone 17" "$BUNDLE_ID"
```

起動直後 (スプラッシュ表示中) にスクリーンショットを撮る。スプラッシュは短いので `launch` の直後に連続で撮る:

```bash
xcrun simctl io "iPhone 17" screenshot /private/tmp/claude-501/-Users-shinya-workspace-claude-LeafTimer/aaa7ab20-a7de-42c1-9088-8ece7c643b3c/scratchpad/quickwin-task3-dark-1.png; \
xcrun simctl io "iPhone 17" screenshot /private/tmp/claude-501/-Users-shinya-workspace-claude-LeafTimer/aaa7ab20-a7de-42c1-9088-8ece7c643b3c/scratchpad/quickwin-task3-dark-2.png
```

Expected: 濃い緑 (`RGB ≈ 0.18, 0.22, 0.18`) の背景に splashIcon。淡緑が一瞬でも出ていたら asset の解決に失敗している。**真っ黒**なら named color が解決できていない (= colorset の置き場所が違う)。

ライトモードも同様に確認:

```bash
xcrun simctl ui "iPhone 17" appearance light; \
xcrun simctl uninstall "iPhone 17" "$BUNDLE_ID"; \
xcrun simctl install "iPhone 17" "$APP_PATH"; \
xcrun simctl launch "iPhone 17" "$BUNDLE_ID"; \
xcrun simctl io "iPhone 17" screenshot /private/tmp/claude-501/-Users-shinya-workspace-claude-LeafTimer/aaa7ab20-a7de-42c1-9088-8ece7c643b3c/scratchpad/quickwin-task3-light-1.png
```

Expected: 従来どおりの淡緑背景 (回帰していないこと)。

撮影した4枚は `SendUserFile` でユーザーに渡す (PR 本文にローカルパスの画像は埋め込めない — GitHub は到達可能な URL しか取得しない)。

- [ ] **Step 5: commit**

```bash
cd /Users/shinya/workspace/claude/LeafTimer && \
git add app/LeafTimer/App/Assets.xcassets/LaunchBackground.colorset/Contents.json \
        app/LeafTimer/App/Base.lproj/LaunchScreen.storyboard && \
git commit -m "fix(launch): #67 LaunchScreen をライト/ダーク両対応の named color にする

固定の淡緑背景のため、ダークモード端末で起動時に淡緑→濃色のフラッシュが出ていた。
LaunchBackground colorset (light: 従来の淡緑 / dark: トップ画面の work 濃緑上端) を
pbxproj 登録済みの App/Assets.xcassets に追加し、storyboard から参照する。
IB キャンバス設定にすぎない appearance=\"light\" も併せて削除。"
```

---

### Task 4: #72 — 本番デッドコードの削除とテストの実体化

**Files:**
- Delete: `app/LeafTimer/View/SettingView.swift`
- Modify: `app/LeafTimer.xcodeproj/project.pbxproj` (SettingView.swift の4エントリ削除)
- Modify: `app/LeafTimer/View/AdsView.swift` (末尾の `ContentView` / `ContentView_Previews` 削除)
- Modify: `app/LeafTimerTests/ModernSettingViewSpec.swift` (SettingView 依存テストの整理 + 重複 extension 削除)

**Interfaces:**
- Consumes: なし
- Produces: なし (削除のみ)

#### 背景 (実装者向け) — issue 本文に無い重要な発見が2つ

**(1) `ModernSettingViewSpec.swift` は削除してはいけない。** issue は「テストの EnhancedSettingView への移植とセット」と書いているが、実際にファイルを読むと:

- `MockUserDefaultsWrapper` クラス (ファイル末尾付近) が定義されており、**`OnboardingGateTests.swift` がこれを使っている**。ファイルごと消すと別テストが壊れる。
- SettingView を `inspect()` している `it` は、中身が `expect(x) != nil` / `expect(true) == true` の**実質的に何も検証していないもの**が大半。特に `it("uses NavigationStack for iOS 17 compatibility")` は、`NavigationView` を使った旧 `SettingView` に対して `.navigationView()` を検査しており、**テスト名と検証内容が正反対**。
- 一方 `State Management` / `Reset Functionality` / `Preview Mode` の各テストは `settingView` を一切使わず ViewModel のみを検証しており、**これらは本物の価値がある**。

よってファイル削除ではなく**編集**する。

**(2) `resetToDefaults()` が二重定義になっていて、テストが本番コードを検証していない。** 🚨

- 本番: `app/LeafTimer/View/Settings/ResetSettingsSection.swift:78` の `extension SettingViewModel { func resetToDefaults() }`
- テスト: `app/LeafTimerTests/ModernSettingViewSpec.swift` 末尾にも**同じシグネチャの extension**

テストターゲットは `@testable import LeafTimer` で別 module のため redeclaration エラーにはならず、テストモジュール内の定義が本番の定義を shadow する。つまり `it("resets all settings to default values")` は**テストが自分で書いた実装をテストしている** — 本番の `ResetSettingsSection.swift:78` は一度も実行されたことがない。テスト側の extension を削除すると、初めて本番実装が検証対象になる。

両者の実装は挙動が同一 (どちらも workingTime=4 / breakTime=4 / vibrationIsOn=true) なので、削除してもテストは通る想定。**もし落ちたら、それは本番実装のバグを初めて発見したということ**なので、その内容を報告すること (勝手に本番側を書き換えない)。

- [ ] **Step 1: テストの重複 extension を削除して RED/GREEN を確認する**

まず現状のテストが通っていることを確認する (ベースライン):

```bash
cd /Users/shinya/workspace/claude/LeafTimer/app && set -o pipefail && \
make unit-tests 2>&1 | tee /private/tmp/claude-501/-Users-shinya-workspace-claude-LeafTimer/aaa7ab20-a7de-42c1-9088-8ece7c643b3c/scratchpad/quickwin-task4-before.log | grep -c "\*\* TEST SUCCEEDED \*\*"
```

(Bash tool の `timeout` は `600000`)

Expected: `1` 以上。

次に `app/LeafTimerTests/ModernSettingViewSpec.swift` の**末尾にある次のブロックだけ**を削除する:

```swift
// Extension for ViewModel reset functionality
extension SettingViewModel {
    func resetToDefaults() {
        workingTime = 4  // 25 minutes
        breakTime = 4    // 5 minutes
        workingSound = 0
        breakSound = 0
        vibrationIsOn = true
        mode = 0

        // Save to UserDefaults
        write(selected: workingTime, item: UserDefaultItem.workingTime.rawValue)
        write(selected: breakTime, item: UserDefaultItem.breakTime.rawValue)
        write(selected: workingSound, item: UserDefaultItem.workingSound.rawValue)
        write(selected: breakSound, item: UserDefaultItem.breakSound.rawValue)
        write(isOn: vibrationIsOn, item: UserDefaultItem.vibration.rawValue)
    }
}
```

⚠️ **`MockUserDefaultsWrapper` クラスは絶対に消さないこと** (`OnboardingGateTests.swift` が使用)。

- [ ] **Step 2: テストを実行し、本番実装が検証対象になったことを確認する**

```bash
cd /Users/shinya/workspace/claude/LeafTimer/app && set -o pipefail && \
make unit-tests 2>&1 | tee /private/tmp/claude-501/-Users-shinya-workspace-claude-LeafTimer/aaa7ab20-a7de-42c1-9088-8ece7c643b3c/scratchpad/quickwin-task4-step2.log | tail -30; \
grep -c "\*\* TEST SUCCEEDED \*\*" /private/tmp/claude-501/-Users-shinya-workspace-claude-LeafTimer/aaa7ab20-a7de-42c1-9088-8ece7c643b3c/scratchpad/quickwin-task4-step2.log
```

Expected: `** TEST SUCCEEDED **`。`it("resets all settings to default values")` が今度は `ResetSettingsSection.swift:78` の本番実装を実行している。

もし FAIL したら: 本番実装と旧テスト実装の挙動差を初めて検出したということ。差分を報告し、**勝手に本番側を書き換えず指示を仰ぐ**。

- [ ] **Step 3: SettingView 依存のテストを整理する**

`app/LeafTimerTests/ModernSettingViewSpec.swift` から次を削除する。

(a) `describe("Enhanced SettingView")` 直下の変数宣言と初期化:

```swift
            var settingView: SettingView!
```

および `beforeEach` 内の:

```swift
                settingView = SettingView(settingViewModel: settingViewModel)
```

(b) `settingView` を参照している `it` / `xit` を**すべて削除**する (いずれも旧 SettingView の構造を検査しており、EnhancedSettingView には当てはまらない):

- `it("uses NavigationStack for iOS 17 compatibility")` — 名前と逆に `NavigationView` を検査している嘘テスト
- `xit("has properly grouped settings sections")`
- `it("displays section headers with icons")`
- `xit("provides inline stepper controls for time settings")`
- `it("provides sound preview button for each option")`
- `it("uses modern list style with insets")`
- `it("has accessibility labels for all controls")`

(c) 代わりに、**本番導線の `EnhancedSettingView` が実際に構築できる**ことを検証する smoke test を `describe("Modern Layout")` 内に追加する:

```swift
                it("builds the production settings screen") {
                    let view = EnhancedSettingView(settingViewModel: settingViewModel)
                    expect { try view.inspect().find(ViewType.Form.self) }.toNot(throwError())
                }
```

`find(_:)` は階層を再帰探索するため、`NavigationStack` 用の ViewInspector API に依存しない。

⚠️ **もしこの smoke test が失敗する場合** (`EnhancedSettingView` は body 内に `AdsView` を含み、`AdsBootstrapper.shared` に触れる): 深追いせず smoke test を削除し、ViewModel 系テストのみを残して次に進む。その旨を報告すること。デッドコード削除が本題であり、テスト新規追加は副次的な改善。

(d) `describe("Enhanced SettingView")` という名前は、`EnhancedSettingView` を検証するようになったことで実態と一致する。ファイル名 `ModernSettingViewSpec.swift` のリネームは `project.pbxproj` の編集を増やすため**今回は行わない**。

- [ ] **Step 4: テストを実行する**

```bash
cd /Users/shinya/workspace/claude/LeafTimer/app && set -o pipefail && \
make unit-tests 2>&1 | tee /private/tmp/claude-501/-Users-shinya-workspace-claude-LeafTimer/aaa7ab20-a7de-42c1-9088-8ece7c643b3c/scratchpad/quickwin-task4-step4.log | tail -30; \
grep -c "\*\* TEST SUCCEEDED \*\*" /private/tmp/claude-501/-Users-shinya-workspace-claude-LeafTimer/aaa7ab20-a7de-42c1-9088-8ece7c643b3c/scratchpad/quickwin-task4-step4.log
```

Expected: `** TEST SUCCEEDED **`。この時点で `SettingView` を参照するテストは存在しない。

- [ ] **Step 5: `SettingView` への参照がテストから消えたことを確認する**

```bash
cd /Users/shinya/workspace/claude/LeafTimer && \
grep -rn "SettingView(" app/LeafTimerTests/ app/LeafTimer/ --include="*.swift" | grep -v "EnhancedSettingView(" | grep -v "SettingViewModel("
```

Expected: `app/LeafTimer/View/SettingView.swift` 内の自身の Preview (`SettingView(settingViewModel: ...)`) **のみ**がヒットする。他が残っていたら Step 3 の削除漏れ。

- [ ] **Step 6: `SettingView.swift` を削除する**

```bash
cd /Users/shinya/workspace/claude/LeafTimer && git rm app/LeafTimer/View/SettingView.swift
```

- [ ] **Step 7: `project.pbxproj` から4つのエントリを削除する**

`app/LeafTimer.xcodeproj/project.pbxproj` から `SettingView.swift` を含む行を**4箇所すべて**削除する (`EnhancedSettingView.swift` の行と間違えないこと — 別 UUID):

```
		3865AB4524CD2272001B03E4 /* SettingView.swift in Sources */ = {isa = PBXBuildFile; fileRef = 3865AB4424CD2272001B03E4 /* SettingView.swift */; };
		3865AB4424CD2272001B03E4 /* SettingView.swift */ = {isa = PBXFileReference; lastKnownFileType = sourcecode.swift; path = SettingView.swift; sourceTree = "<group>"; };
				3865AB4424CD2272001B03E4 /* SettingView.swift */,
				3865AB4524CD2272001B03E4 /* SettingView.swift in Sources */,
```

削除後に確認:

```bash
cd /Users/shinya/workspace/claude/LeafTimer/app && \
grep -n "SettingView.swift" LeafTimer.xcodeproj/project.pbxproj
```

Expected: `EnhancedSettingView.swift` の行のみがヒットする (4行)。UUID `3865AB44` / `3865AB45` が1つも残っていないこと:

```bash
cd /Users/shinya/workspace/claude/LeafTimer/app && \
grep -c "3865AB44\|3865AB45" LeafTimer.xcodeproj/project.pbxproj
```

Expected: `0`

- [ ] **Step 8: `AdsView.swift` からデッドな `ContentView` を削除する**

`app/LeafTimer/View/AdsView.swift` の末尾にある次の2つの struct を削除する (Preview からしか参照されておらず、本番導線は `AppDelegate.swift:25` → `TimerView.swift` で `ContentView` を通らない):

```swift
struct ContentView: View {
    var body: some View {
        //        AdsView()
        // サイズを変更する場合
        AdsView().frame(width: 320, height: 50)
    }
}

struct ContentView_Previews: PreviewProvider {
    static var previews: some View {
        ContentView()
    }
}
```

`AdsView` と `AdsBannerView` は本番で使われているので**残す**。

- [ ] **Step 9: precheck とフルテストを実行する**

`project.pbxproj` を編集したので orphan 検出も走らせる:

```bash
cd /Users/shinya/workspace/claude/LeafTimer/app && set -o pipefail && \
make tests 2>&1 | tee /private/tmp/claude-501/-Users-shinya-workspace-claude-LeafTimer/aaa7ab20-a7de-42c1-9088-8ece7c643b3c/scratchpad/quickwin-task4-final.log | tail -40
```

(Bash tool の `timeout` は `600000`)

Expected: 出力に `** TEST SUCCEEDED **` があり、precheck が新規 orphan を報告していないこと。SwiftLint の違反もゼロであること。次で確認:

```bash
grep -c "\*\* TEST SUCCEEDED \*\*" /private/tmp/claude-501/-Users-shinya-workspace-claude-LeafTimer/aaa7ab20-a7de-42c1-9088-8ece7c643b3c/scratchpad/quickwin-task4-final.log; \
grep -c "✅ xcode-precheck passed" /private/tmp/claude-501/-Users-shinya-workspace-claude-LeafTimer/aaa7ab20-a7de-42c1-9088-8ece7c643b3c/scratchpad/quickwin-task4-final.log; \
grep -c "\*\* TEST FAILED \*\*\|Error 6\|❌" /private/tmp/claude-501/-Users-shinya-workspace-claude-LeafTimer/aaa7ab20-a7de-42c1-9088-8ece7c643b3c/scratchpad/quickwin-task4-final.log
```

Expected: 1行目 `1` 以上、2行目 `1`、3行目 `0`。

⚠️ **失敗マーカーに `orphan` という単語を使わないこと。** precheck は**成功時にも** `✅ targets: no new orphan Swift files (0 grandfathered in baseline)` と出力する。`grep -c "orphan"` は正常時も非ゼロを返すため、GREEN を RED と誤読する。precheck の判定は成功マーカー `✅ xcode-precheck passed` の存在と、失敗マーカー `❌` の不在で行う (実測した成功時出力は3行すべて `✅` 始まり、exit 0)。

- [ ] **Step 10: `make sort` を実行して pbxproj を整列する**

```bash
cd /Users/shinya/workspace/claude/LeafTimer/app && make sort && \
cd /Users/shinya/workspace/claude/LeafTimer && git status --short
```

Expected: `make sort` による追加の差分が出た場合はそれも次の commit に含める。**この確認を飛ばすと PR 作成直後に "1 uncommitted change" warning として表面化する** (Issue #8 の実績)。

- [ ] **Step 11: commit**

```bash
cd /Users/shinya/workspace/claude/LeafTimer && \
git add app/LeafTimer/View/AdsView.swift app/LeafTimerTests/ModernSettingViewSpec.swift \
        app/LeafTimer.xcodeproj/project.pbxproj && \
git add -u app/LeafTimer/View/SettingView.swift && \
git commit -m "refactor: #72 本番デッドコード (旧 SettingView / AdsView 内 ContentView) を削除

本番導線は AppDelegate → TimerView → EnhancedSettingView で、旧 SettingView は
自身の Preview とテスト1件からしか参照されていなかった。

あわせてテスト側の重複 extension SettingViewModel.resetToDefaults() を削除した。
これはテストモジュール内で本番の ResetSettingsSection.swift:78 を shadow しており、
reset 系テストが本番実装を一度も実行していなかった (テストが自分の実装を検証していた)。
削除により初めて本番実装が検証対象になる。

MockUserDefaultsWrapper は OnboardingGateTests が使用しているため残置。"
```

---

### Task 5: 最終検証と PR 作成

**Files:** なし (検証と PR のみ)

- [ ] **Step 1: 全テストを最終実行する**

```bash
cd /Users/shinya/workspace/claude/LeafTimer/app && set -o pipefail && \
make tests 2>&1 | tee /private/tmp/claude-501/-Users-shinya-workspace-claude-LeafTimer/aaa7ab20-a7de-42c1-9088-8ece7c643b3c/scratchpad/quickwin-final.log | tail -40
```

(Bash tool の `timeout` は `600000`)

Expected: `** TEST SUCCEEDED **`。

- [ ] **Step 2: Ruby スクリプトのテストも実行する**

```bash
cd /Users/shinya/workspace/claude/LeafTimer/app && \
ruby bin/test_xcode_precheck.rb && ruby bin/test_gitignore_doctor.rb
```

Expected: 両方 `0 failures, 0 errors`。

- [ ] **Step 3: 作業ツリーがクリーンであることを確認する**

```bash
cd /Users/shinya/workspace/claude/LeafTimer && git status --short && git log --oneline master..HEAD
```

Expected: `git status` の出力が空。commit が5本 (plan / #80 / #65 / #67 / #72) 並ぶこと。

補足: `.claude/pending-reflection.md` は **gitignore 済みの untracked ファイル** (`git status --ignored --short .claude/` で `!!` と表示されることを確認済み)。ユーザーが手元で `rm` してもこの `git status` には現れないので、差分を探しに行かないこと。

- [ ] **Step 4: Simulator で4状態を目視確認する**

設定画面 (Volume 行が消えたこと) と LaunchScreen (light/dark) を確認する。バンドルパスは絶対パス:

```bash
cd /Users/shinya/workspace/claude/LeafTimer/app && \
BUILT_DIR=$(xcodebuild -workspace LeafTimer.xcworkspace -scheme LeafTimer -destination "platform=iOS Simulator,name=iPhone 17,OS=latest" -showBuildSettings 2>/dev/null | grep -m1 "BUILT_PRODUCTS_DIR" | sed 's/.*= //'); \
APP_PATH="$BUILT_DIR/LeafTimer.app"; \
BUNDLE_ID=$(/usr/libexec/PlistBuddy -c "Print CFBundleIdentifier" "$APP_PATH/Info.plist"); \
for mode in light dark; do \
  xcrun simctl ui "iPhone 17" appearance $mode; \
  xcrun simctl uninstall "iPhone 17" "$BUNDLE_ID" 2>/dev/null; \
  xcrun simctl install "iPhone 17" "$APP_PATH"; \
  xcrun simctl launch "iPhone 17" "$BUNDLE_ID"; \
  xcrun simctl io "iPhone 17" screenshot /private/tmp/claude-501/-Users-shinya-workspace-claude-LeafTimer/aaa7ab20-a7de-42c1-9088-8ece7c643b3c/scratchpad/quickwin-final-$mode.png; \
done; ls -la /private/tmp/claude-501/-Users-shinya-workspace-claude-LeafTimer/aaa7ab20-a7de-42c1-9088-8ece7c643b3c/scratchpad/quickwin-final-*.png
```

撮影した画像は `SendUserFile` でユーザーに渡す。

- [ ] **Step 5: 既存 PR を確認してから push する**

```bash
cd /Users/shinya/workspace/claude/LeafTimer && git fetch && \
gh pr list --state all --head feature/quick-wins-65-67-80-72
```

Expected: 出力が空 (既存 PR なし)。空を確認してから:

```bash
cd /Users/shinya/workspace/claude/LeafTimer && \
git push -u origin feature/quick-wins-65-67-80-72
```

- [ ] **Step 6: PR を作成する**

```bash
cd /Users/shinya/workspace/claude/LeafTimer && gh pr create --base master \
  --title "fix: 小粒 quick win まとめ (#80 #65 #67 #72)" --body "$(cat <<'EOF'
2026-07-06 のリポジトリ総点検で起票された小粒 issue のうち、互いに独立した4件をまとめて解消します。

## 対応内容

| Issue | 内容 |
| --- | --- |
| #80 | シミュレータ名を `Makefile` の `SIMULATOR` 変数に一元化。Fastfile が Makefile から読むようにし、実在しない `LeafTimer Release` scheme 参照を修正 |
| #65 | 常時 disabled の Volume ダミースライダーを本番 UI から削除 |
| #67 | LaunchScreen をライト/ダーク両対応の named color にし、ダーク起動時のフラッシュを解消 |
| #72 | 本番デッドコード (旧 `SettingView.swift` / `AdsView` 内 `ContentView`) を削除 |

## 実装中に判明した、issue 本文に無い発見

1. **#80 は XS ではなく S だった** — `bin/xcode_precheck.rb` が Makefile の `-destination` 行をテキストパースしているため、素朴に変数化すると `make precheck` が `$(SIMULATOR)` をシミュレータ名と誤認して RED になる。`destination_name` に Make 変数解決を追加し、minitest で正常系・異常系の両方を検証した。

2. **テストが本番の `resetToDefaults()` を一度も実行していなかった** — `ModernSettingViewSpec.swift` 側に `extension SettingViewModel { resetToDefaults() }` が定義されており、`@testable import` 越しに本番の `ResetSettingsSection.swift:78` を shadow していた。テスト側の extension を削除し、初めて本番実装が検証対象になった。

3. **`app/LeafTimer/Assets.xcassets` (Colors/ の14 colorset) は pbxproj に未登録** — どの target にも attach されておらずビルドに含まれない。これは Issue #63 の「未使用の14個のセマンティックカラー資産」の正体。本 PR の新規 colorset は登録済みの `App/Assets.xcassets` 側に置いた。

## 検証

- `make tests` (unit-tests + precheck + lint) が green
- `ruby bin/test_xcode_precheck.rb` が green (Make 変数解決の新規テスト4件を含む)
- 意図的に存在しないシミュレータ名を入れて `make precheck` が RED になることを実証
- Simulator で light/dark 両モードの LaunchScreen を uninstall→install して目視確認

## スクリーンショット

<!-- ここにユーザーがブラウザからドラッグ&ドロップで添付 -->

Closes #80
Closes #65
Closes #67
Closes #72

🤖 Generated with [Claude Code](https://claude.com/claude-code)
EOF
)"
```

- [ ] **Step 7: スクリーンショットをユーザーに渡す**

`gh pr create` の本文には**ローカルファイルパスの画像を埋め込めない** (GitHub は到達可能な URL しか取得しない)。`SendUserFile` で `/private/tmp/claude-501/-Users-shinya-workspace-claude-LeafTimer/aaa7ab20-a7de-42c1-9088-8ece7c643b3c/scratchpad/quickwin-final-light.png` と `/private/tmp/claude-501/-Users-shinya-workspace-claude-LeafTimer/aaa7ab20-a7de-42c1-9088-8ece7c643b3c/scratchpad/quickwin-final-dark.png` をユーザーに渡し、PR 説明欄の「スクリーンショット」セクションにブラウザからドラッグ&ドロップで添付してもらう。

---

## Self-Review

**1. Issue coverage:**

| Issue | 対応タスク | 対応方針案との一致 |
| --- | --- | --- |
| #80 destination 変数化 | Task 1 Step 5 | ✅ (+ precheck 側の対応を追加) |
| #80 Fastfile / doc の scheme 修正 | Task 1 Step 8, 10 | ✅ |
| #65 Volume ブロック非表示 | Task 2 Step 1 | ✅ (+ 宙に浮く footer 文言も修正) |
| #67 named color 差し替え | Task 3 Step 1-2 | ✅ |
| #67 固定 appearance 指定を外す | Task 3 Step 2(a) | ✅ (実行時には無害だが issue の指摘どおり削除) |
| #72 SettingView.swift 削除 | Task 4 Step 6-7 | ✅ |
| #72 AdsView 内 ContentView 削除 | Task 4 Step 8 | ✅ |
| #72 「テストの移植とセット」 | Task 4 Step 1-3 | ⚠️ **方針変更**: 移植ではなく整理。理由は Task 4 の背景に記載 (対象テストの大半が `expect(true) == true` の空テストで、移植する価値のある中身が無い。ViewModel 系テストは元から `settingView` に依存しておらず、そのまま残る) |
| #72 「#4 マージ後の着手が安全」 | — | ✅ #4 は 2026-07-07 に CLOSED 済み。前提クリア |

**2. Placeholder scan:** 「適切にエラーハンドリング」「TBD」「Task N と同様」等のプレースホルダは無し。全コードブロックに実際の内容を記載済み。

**3. Type consistency:**
- `XcodePrecheck.destination_name` / `XcodePrecheck.expand_variable` — Task 1 の Interfaces で定義した名前が Step 1 (テスト) / Step 3 (実装) / Step 9 (Fastfile) で一致している ✅
- `SIMULATOR` — Makefile (Step 5) / テスト (Step 1) / precheck の解決対象 (Step 6-7) で一致 ✅
- `SIMULATOR_NAME` — Fastfile 内の定数名。Step 8 の定義と `scan(device:)` の参照で一致 ✅
- `LaunchBackground` — colorset ディレクトリ名 (Step 1) / storyboard の `name` 属性 (Step 2b) / `<namedColor name=>` (Step 2c) で一致 ✅
- `MockUserDefaultsWrapper` — Task 4 で「残す」と明記した名前が、`OnboardingGateTests.swift` の利用側と一致 ✅

**4. 前提の実在確認:** この plan が参照する tool / path は全て実行前に Glob / grep で実在を確認済み:
- `app/bin/xcode_precheck.rb` / `app/bin/test_xcode_precheck.rb` / `app/bin/xcode-precheck.rb` ✅
- `app/bin/sortXcodeProject` (= `make sort` の実体) ✅
- `app/LeafTimer/App/Assets.xcassets` が pbxproj 登録済みの唯一のカタログであること ✅
- `docs/ver1_2/xcode-cloud-setup.md:50` の `LeafTimer Release` 記述 ✅
