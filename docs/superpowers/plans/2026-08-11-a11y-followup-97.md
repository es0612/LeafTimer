# a11y follow-up (#97): 残り時間の VoiceOver 読み上げ最適化とラベル無し Toggle/Picker のラベル付与 — Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** VoiceOver が残り時間を「25分30秒」のような自然な時間表現で読み上げ、設定画面の無名 Toggle/Picker に名前が付く状態にする (Issue #97、PR #96 の後送 follow-up M6/M8)。

**Architecture:** PR #96 で確立した「a11y 文字列は ViewModel へ集約」パターンに従い、残り時間の読み上げ値は `TimerViewModel+extensions.swift` に `getAccessibilityTimeValue(locale:)` として追加する。時間表現の生成は `DateComponentsFormatter` (`unitsStyle: .full`) に委譲し、ja「25分30秒」/ en「25 minutes, 30 seconds」と単数複数 (1 minute / 2 minutes) をローカライズごと OS に任せる。設定画面のラベルは状態非依存の静的文字列なので View に直接 `.accessibilityLabel` を付与し、**既存の可視ラベルと同じ L10n キーを再利用**する。

**Tech Stack:** SwiftUI / XCTest / DateComponentsFormatter / NSLocalizedString (ja・en .lproj)

## Global Constraints

- **新規ファイルを作らない。** 実装は既存 `app/LeafTimer/ViewModel/TimerViewModel+extensions.swift`・`app/LeafTimer/View/TimerView.swift`・`app/LeafTimer/View/Settings/SoundSettingsSection.swift`・`app/LeafTimer/View/Settings/TimerSettingsSection.swift`、テストは既存 `app/LeafTimerTests/TimerViewModelAccessibilityTests.swift` への追記で完結させる (pbxproj 変更なし → orphan / `make sort` / `make precheck` の心配を丸ごと回避)。
- **新規 L10n キーを追加しない。** accessibilityValue は時間表現のみ (label「残り時間」`timer.a11y.remaining_time` と VoiceOver が合成して「残り時間、25分」と読む)。設定コントロールは既存キー `settings.vibration` / `settings.working_time` / `settings.break_time` を再利用。`SettingsLocalizationTests` も触らない。
- **DateComponentsFormatter の期待値は RED 実行で観測してからピン留めする。** ja 出力のスペース有無 (「25分30秒」vs「25分 30秒」) は ICU/OS バージョン依存のため、推測で exact assert を書かない。RED 段階の実際の失敗メッセージ (actual 値) を見てから期待値を確定する。CI (GitHub Actions, PR #94 ゲート) の simulator runtime がローカルと違って出力が割れた場合のみ `XCTAssertTrue(value.contains("25"))` 形式に緩める。
- テスト実行は `cd app && make unit-tests` (Bash timeout 600000)。成否は exit code ではなく出力マーカー (`** TEST SUCCEEDED **` / `** TEST FAILED **`) で判定する (zsh の `${PIPESTATUS[0]}` は使えない)。
- コミットは各 Task 末尾で行い、対象ファイルを明示的に `git add <path>` する (`git add -A` 禁止)。

---

### Task 1: `getAccessibilityTimeValue(locale:)` を TDD で追加

**Files:**
- Modify: `app/LeafTimer/ViewModel/TimerViewModel+extensions.swift` (getAccessibilityLabel の直後、48行目付近に追加)
- Test: `app/LeafTimerTests/TimerViewModelAccessibilityTests.swift` (末尾に追加)

**Interfaces:**
- Consumes: `TimerViewModel.currentTimeSecond: Int` (internal var、@testable でテストから直接設定可能)、既存 `getDisplayedTime() -> String`
- Produces: `func getAccessibilityTimeValue(locale: Locale = .current) -> String` — Task 2 の `TimerView` がデフォルト引数で呼ぶ。ja で `1500 → "25分"` / `1530 → "25分30秒"` / `59 → "59秒"` / `0 → "0秒"` 相当 (exact 値は RED で観測後にピン留め)

- [ ] **Step 1: Write the failing tests**

`TimerViewModelAccessibilityTests.swift` の class 末尾 (39行目 `}` の前) に追加:

```swift
    // MARK: - Issue #97: 残り時間の読み上げ値 (M6)

    // 期待値は RED 実行の actual 出力でピン留めする (ICU の空白/区切りは OS 依存のため推測禁止)。
    private let ja = Locale(identifier: "ja_JP")
    private let en = Locale(identifier: "en_US")

    func testAccessibilityTimeValueMinutesOnlyJapanese() {
        let viewModel = makeViewModel()
        viewModel.currentTimeSecond = 25 * 60

        XCTAssertEqual(viewModel.getAccessibilityTimeValue(locale: ja), "25分")
    }

    func testAccessibilityTimeValueMinutesAndSecondsJapanese() {
        let viewModel = makeViewModel()
        viewModel.currentTimeSecond = 25 * 60 + 30

        XCTAssertEqual(viewModel.getAccessibilityTimeValue(locale: ja), "25分30秒")
    }

    func testAccessibilityTimeValueMinutesAndSecondsEnglish() {
        let viewModel = makeViewModel()
        viewModel.currentTimeSecond = 25 * 60 + 30

        XCTAssertEqual(viewModel.getAccessibilityTimeValue(locale: en), "25 minutes, 30 seconds")
    }

    func testAccessibilityTimeValueSingularMinuteEnglish() {
        let viewModel = makeViewModel()
        viewModel.currentTimeSecond = 60

        XCTAssertEqual(viewModel.getAccessibilityTimeValue(locale: en), "1 minute")
    }

    func testAccessibilityTimeValueSecondsOnlyJapanese() {
        let viewModel = makeViewModel()
        viewModel.currentTimeSecond = 59

        XCTAssertEqual(viewModel.getAccessibilityTimeValue(locale: ja), "59秒")
    }

    func testAccessibilityTimeValueZeroJapanese() {
        let viewModel = makeViewModel()
        viewModel.currentTimeSecond = 0

        XCTAssertEqual(viewModel.getAccessibilityTimeValue(locale: ja), "0秒")
    }
```

- [ ] **Step 2: Run tests to verify they fail (compile error)**

Run: `cd /Users/shinya/workspace/claude/LeafTimer/app && make unit-tests 2>&1 | tee /tmp/task1-red.log; grep -c "TEST FAILED\|error:" /tmp/task1-red.log` (Bash timeout 600000)

Expected: コンパイルエラー `value of type 'TimerViewModel' has no member 'getAccessibilityTimeValue'` で FAIL。

- [ ] **Step 3: Write the implementation**

`TimerViewModel+extensions.swift` の `getAccessibilityLabel()` (40-48行目) の直後に追加:

```swift
    // Issue #97: 残り時間の VoiceOver 読み上げ値。"25:00" の数字列読みを避け、
    // DateComponentsFormatter に単数複数・ロケール処理ごと委譲する。
    // 毎秒呼ばれるが formatter は軽量で、locale 注入 (テスト決定性) と両立させるため都度生成のままにする。
    func getAccessibilityTimeValue(locale: Locale = .current) -> String {
        let formatter = DateComponentsFormatter()
        var calendar = Calendar(identifier: .gregorian)
        calendar.locale = locale
        formatter.calendar = calendar
        formatter.unitsStyle = .full
        if currentTimeSecond < 60 {
            // 0 を「0秒」と読ませるため zero を drop しない
            formatter.allowedUnits = [.second]
        } else {
            formatter.allowedUnits = [.minute, .second]
            formatter.zeroFormattingBehavior = .dropTrailing
        }
        return formatter.string(from: TimeInterval(currentTimeSecond)) ?? getDisplayedTime()
    }
```

先頭 import が `import SwiftUI` のみであることを確認 (DateComponentsFormatter は Foundation。SwiftUI が Foundation を再輸出するのでビルドは通るはずだが、エラーになる場合のみ `import Foundation` を追加)。

- [ ] **Step 4: Run tests, observe actual values, pin expectations**

Run: `cd /Users/shinya/workspace/claude/LeafTimer/app && make unit-tests 2>&1 | tee /tmp/task1-green.log; grep -E "TEST (SUCCEEDED|FAILED)" /tmp/task1-green.log` (Bash timeout 600000)

Expected: `** TEST SUCCEEDED **`。もし assert 失敗が出た場合、それは ICU 出力の表記揺れ (空白・区切り) なので、**failure message の actual 値をテストの期待値へ転記**して再実行。actual が明らかに壊れている場合 (空文字等) のみ実装を疑う。

- [ ] **Step 5: Commit**

```bash
cd /Users/shinya/workspace/claude/LeafTimer
git add app/LeafTimer/ViewModel/TimerViewModel+extensions.swift app/LeafTimerTests/TimerViewModelAccessibilityTests.swift
git commit -m "feat(a11y): #97 残り時間の読み上げ値 getAccessibilityTimeValue を追加"
```

---

### Task 2: TimerView の accessibilityValue 差し替えと updatesFrequently 付与

**Files:**
- Modify: `app/LeafTimer/View/TimerView.swift:59-60`

**Interfaces:**
- Consumes: Task 1 の `getAccessibilityTimeValue()` (デフォルト引数 `.current` で呼ぶ)
- Produces: なし (View 最終段)

- [ ] **Step 1: Edit TimerView**

`TimerView.swift` 59-60行目:

```swift
                        .accessibilityLabel(NSLocalizedString("timer.a11y.remaining_time", comment: "Remaining time label"))
                        .accessibilityValue(timerViewModel.getDisplayedTime())
```

を以下に置換:

```swift
                        .accessibilityLabel(NSLocalizedString("timer.a11y.remaining_time", comment: "Remaining time label"))
                        .accessibilityValue(timerViewModel.getAccessibilityTimeValue())
                        .accessibilityAddTraits(.updatesFrequently)
```

- [ ] **Step 2: Run tests to verify nothing broke**

Run: `cd /Users/shinya/workspace/claude/LeafTimer/app && make unit-tests 2>&1 | tee /tmp/task2.log; grep -E "TEST (SUCCEEDED|FAILED)" /tmp/task2.log` (Bash timeout 600000)

Expected: `** TEST SUCCEEDED **` (この変更を直接検証する unit test は無い — ViewInspector は AdsBannerView の traversal blocker があるため使わない。実機挙動は Task 4 の監査 routing で担保)。

- [ ] **Step 3: Commit**

```bash
cd /Users/shinya/workspace/claude/LeafTimer
git add app/LeafTimer/View/TimerView.swift
git commit -m "feat(a11y): #97 残り時間 Text に読み上げ最適化 value と updatesFrequently を配線"
```

---

### Task 3: 設定画面のラベル無し Toggle/Picker にラベル付与

**Files:**
- Modify: `app/LeafTimer/View/Settings/SoundSettingsSection.swift:70-83` (バイブレーション Toggle)
- Modify: `app/LeafTimer/View/Settings/TimerSettingsSection.swift:27-40` (作業時間 Picker), `:62-75` (休憩時間 Picker)

**Interfaces:**
- Consumes: 既存 L10n キー `settings.vibration` / `settings.working_time` / `settings.break_time` (ja/en 両 .lproj に存在済み)
- Produces: なし

- [ ] **Step 1: SoundSettingsSection の Toggle にラベル付与**

`SoundSettingsSection.swift` の Toggle チェーン末尾 (82-83行目):

```swift
                .labelsHidden()
                .tint(.blue)
```

を以下に置換:

```swift
                .labelsHidden()
                .tint(.blue)
                .accessibilityLabel(NSLocalizedString("settings.vibration", comment: "Vibration setting"))
```

- [ ] **Step 2: TimerSettingsSection の 2 Picker にラベル付与**

作業時間 Picker のチェーン末尾 (39-40行目):

```swift
                .pickerStyle(.menu)
                .labelsHidden()
```

を以下に置換 (1つ目 = 作業時間側):

```swift
                .pickerStyle(.menu)
                .labelsHidden()
                .accessibilityLabel(NSLocalizedString("settings.working_time", comment: "Working time setting"))
```

休憩時間 Picker のチェーン末尾 (74-75行目、同じ `.pickerStyle(.menu)` + `.labelsHidden()` の2つ目) を以下に置換:

```swift
                .pickerStyle(.menu)
                .labelsHidden()
                .accessibilityLabel(NSLocalizedString("settings.break_time", comment: "Break time setting"))
```

- [ ] **Step 3: Run tests**

Run: `cd /Users/shinya/workspace/claude/LeafTimer/app && make unit-tests 2>&1 | tee /tmp/task3.log; grep -E "TEST (SUCCEEDED|FAILED)" /tmp/task3.log` (Bash timeout 600000)

Expected: `** TEST SUCCEEDED **`。

- [ ] **Step 4: Commit**

```bash
cd /Users/shinya/workspace/claude/LeafTimer
git add app/LeafTimer/View/Settings/SoundSettingsSection.swift app/LeafTimer/View/Settings/TimerSettingsSection.swift
git commit -m "fix(a11y): #97 ラベル無し Toggle/Picker に既存キー再利用で accessibilityLabel を付与"
```

---

### Task 4: 総合検証・PR 作成・監査 routing

**Files:**
- なし (検証と PR 作成のみ)

**Interfaces:**
- Consumes: Task 1-3 の全コミット
- Produces: PR (Closes #97)

- [ ] **Step 1: SwiftLint + フルテスト**

Run: `cd /Users/shinya/workspace/claude/LeafTimer/app && make tests 2>&1 | tee /tmp/task4-full.log; grep -E "TEST (SUCCEEDED|FAILED)|xcode-precheck" /tmp/task4-full.log` (Bash timeout 600000)

Expected: `✅` precheck 通過 + `** TEST SUCCEEDED **`。

- [ ] **Step 2: Push & PR 作成**

```bash
cd /Users/shinya/workspace/claude/LeafTimer
git fetch && gh pr list --state all --head fix/97-a11y-followup   # 既存 PR 確認
git push -u origin fix/97-a11y-followup
```

PR 本文に含める: Summary (M6/M8 の2点)、Test Plan (追加テスト6件 + make tests)、`Closes #97`、および「**未実施の検証**: Accessibility Inspector 監査 (Xcode → Open Developer Tool → Accessibility Inspector → 起動中 Simulator に audit)。ツールバー3アイコンの UIKit 描画でのラベル実効性 (PR #96 I1 の残り) と本 PR の Toggle/Picker ラベルを同時検証する」セクション。

- [ ] **Step 3: Accessibility Inspector 監査の routing をユーザーに確認 (silent drop 禁止)**

Issue #97 コメント (PR #96 final review Rec#2) の監査指示は GUI ツールのためヘッドレス実行不可。`XCUIApplication.performAccessibilityAudit()` 代替は UI テストターゲットが形骸化 (#76) しているため今回は scope 外。PR 作成後に AskUserQuestion (closed 選択式) で「(a) 今この場でユーザーが監査を実施 / (b) PR レビュー時に実施 / (c) 監査は #76 解消後の automated audit へ委ねる (issue コメントに routing を記録)」を確認し、選択結果を PR 本文または Issue #97 コメントに記録する。

---

## Self-Review 済み確認事項

- Issue #97 の 2 要件 (M6: 読み上げ値 + updatesFrequently / M8: Toggle×1 + Picker×2) は Task 1-2 / Task 3 で全カバー。
- Issue コメントの Accessibility Inspector 監査は Task 4 Step 3 で明示 routing (silent drop 禁止)。
- 新規ファイル 0 / 新規 L10n キー 0 / pbxproj 変更 0。
- 型整合: `getAccessibilityTimeValue(locale: Locale = .current) -> String` を Task 1 で定義し Task 2 はデフォルト引数で呼ぶ。
