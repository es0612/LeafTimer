# Issue #128 / #129 / #130 Tech-Debt Bundle Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** #70 後送の小粒 tech-debt 3 件 (#128 ReviewRequesting の @MainActor 契約化 + AppDelegate backgroundTaskID 修正 / #129 ViewInspector index パスの find() 化 / #130 `make add-file` ターゲット化) を 1 ブランチ 1 PR で完済する。

**Architecture:** 3 タスクは互いに独立 (異なるファイル群)。#128 は protocol 境界のみ @MainActor 化し、TimerViewModel 全体の actor 隔離は #69 に委ねる (ユーザー承認済みのスコープ判断、2026-08-28)。#129 はテストのみの変更。#130 は既存 `app/bin/add-to-target.rb` (Issue #3/#15 期から存在、xcodeproj gem 使用・idempotent) を Makefile でラップする — issue 原文の「純粋文字列変換の新規スクリプト」案は既存スクリプトの存在を前提にしていないため採用しない。

**Tech Stack:** Swift 5.9 (concurrency checking minimal) / Quick + Nimble + ViewInspector 0.10.2 / Ruby (xcodeproj gem, minitest) / make

**Spec:** 本 plan が spec を兼ねる。要求の原文は GitHub Issue #128 / #129 / #130 本文 (`gh issue view <N>`)。

## Global Constraints

- ビルド/テストは毎回 `cd /Users/shinya/workspace/claude/LeafTimer/app && make unit-tests` 形式 (cd を同一コマンドに前置)。成否は exit code でなく `** TEST SUCCEEDED **` の存在 + `** TEST FAILED **` / `Error 6x` の不在で判定する (CLAUDE.md ルール 1)。
- `make unit-tests` を含む Bash 呼び出しは timeout 600000 (10 分) を指定する (ルール 16)。
- **本バンドルは新規 Swift ファイルを 1 つも追加しない。** ルール 28 (`make sort` / pbxproj 配線) は発火しない。Task 3 で作る `make add-file` を本バンドル内で実行する必要もない。
- 新規ファイルは Ruby (`app/bin/test_add_to_target.rb`) と Makefile 変更のみ。minitest は `make tests` / `make precheck` のチェーンに **入れない** (CI に xcodeproj gem が無くても green を保つ、ルール 27)。
- 各タスク完了ごとに commit する。commit メッセージは `fix(#128): ...` / `test(#129): ...` / `build(#130): ...` 形式。
- PR merge は plan のスコープ外 (ルール 22)。Task 4 の PR 作成で終了し、merge はレビュー通過後にコントローラが行う。
- Subagent は最終報告の全文を SendMessage で main へ送ってから idle になる (ルール 15)。

---

### Task 0: ブランチ作成 + plan commit (コントローラが実施)

**Files:**
- Create: `docs/superpowers/plans/2026-08-28-128-129-130-tech-debt-bundle.md` (本ファイル)

- [ ] **Step 1: ブランチ作成と plan の first commit**

```bash
cd /Users/shinya/workspace/claude/LeafTimer && git checkout -b feature/128-129-130-tech-debt-bundle && git add docs/superpowers/plans/2026-08-28-128-129-130-tech-debt-bundle.md && git commit -m "docs(#128/#129/#130): tech-debt バンドルの実装 plan を追加"
```

---

### Task 1: #128 ReviewRequesting の @MainActor 契約化 + AppDelegate backgroundTaskID 修正

**Files:**
- Modify: `app/LeafTimer/Components/StoreKitReviewRequester.swift` (protocol + 実装)
- Modify: `app/LeafTimer/ViewModel/TimerViewModel.swift:289-303` (`requestReviewIfNeeded`)
- Modify: `app/LeafTimer/App/AppDelegate.swift:9,70-73`
- **変更禁止:** `app/LeafTimerTests/MockReviewRequester.swift` — nonisolated な同期メソッドは @MainActor protocol 要求を満たせる (安全方向) ため mock はそのままコンパイルが通る。mock に @MainActor を「親切で」足すと、同期 spec コードからの直接呼び出しがコンパイルエラーになる。**触らないこと。**

**Interfaces:**
- Produces: `protocol ReviewRequesting { @MainActor func requestReview(); func openAppStoreReviewPage() }` — 以後、非 MainActor 同期コンテキストから `requestReview()` を呼ぶとコンパイルエラーになる。

- [ ] **Step 1: 契約が本当にコンパイル時検査になることの RED 実証 (commit しない一時コード)**

`StoreKitReviewRequester.swift` の protocol を先に変更する:

```swift
protocol ReviewRequesting {
    @MainActor func requestReview()
    func openAppStoreReviewPage()
}
```

次に `TimerViewModel.swift` の `requestReviewIfNeeded` を**まだ変更しない**ままビルドする:

```bash
cd /Users/shinya/workspace/claude/LeafTimer/app && xcodebuild -workspace 'LeafTimer.xcworkspace' -scheme "LeafTimer" -destination "platform=iOS Simulator,name=iPhone 17,OS=latest" build 2>&1 | grep -E "error:|BUILD (SUCCEEDED|FAILED)" | head -20
```

Expected: `error: call to main actor-isolated instance method 'requestReview()' in a synchronous nonisolated context` (文言は Swift バージョンで多少揺れる) が `TimerViewModel.swift` に出て **BUILD FAILED**。これが「契約がコンパイル時に強制される」ことの実証 (ルール 8/9)。エラーが出ない場合は STOP し、@MainActor が効いていない理由を調べてから進む。

- [ ] **Step 2: StoreKitReviewRequester の実装から assumeIsolated を撤去**

```swift
final class StoreKitReviewRequester: ReviewRequesting {
    @MainActor
    func requestReview() {
        guard let scene = UIApplication.shared.connectedScenes
            .first(where: { $0.activationState == .foregroundActive }) as? UIWindowScene else {
            return
        }
        // Issue #70: SKStoreReviewController.requestReview(in:) は iOS 18 で deprecated。
        // deployment target が iOS 17 のため #available ガードなしで移行できる。
        // Issue #128: main thread 契約は protocol の @MainActor でコンパイル時検査される。
        AppStore.requestReview(in: scene)
    }

    func openAppStoreReviewPage() {
        // (既存のまま変更しない)
    }
}
```

`openAppStoreReviewPage` は issue #128 のスコープ外。触らない。

- [ ] **Step 3: TimerViewModel 側に単一の assumeIsolated を置く**

`requestReviewIfNeeded` の `reviewRequester.requestReview()` **1 行だけ**をラップする (後続の `saveData` は含めない):

```swift
    private func requestReviewIfNeeded(totalCount: Int) {
        let lastRequested: Int = userDefaultWrapper.loadData(
            key: UserDefaultItem.lastReviewRequestedCount.rawValue
        )
        guard reviewPolicy.shouldRequest(
            totalCount: totalCount, lastRequestedCount: lastRequested
        ) else { return }

        // Issue #128: 到達経路は (A) Timer コールバック (DefaultTimerManager が
        // DispatchQueue.main.async で発火) と (B) willEnterForegroundNotification
        // (UIKit が main thread で post) の 2 経路のみで、常に main thread。
        // TimerViewModel 全体の actor 隔離は #69 (@Observable 移行) で行うため、
        // ここでは境界 1 箇所の assumeIsolated に留める。
        MainActor.assumeIsolated {
            reviewRequester.requestReview()
        }
        userDefaultWrapper.saveData(
            key: UserDefaultItem.lastReviewRequestedCount.rawValue,
            value: totalCount
        )
    }
```

- [ ] **Step 4: AppDelegate の backgroundTaskID を 3 点修正**

`app/LeafTimer/App/AppDelegate.swift`。(1) 初期値を `.invalid` に、(2) `applicationDidBecomeActive` に guard、(3) end 後に `.invalid` へリセット。`applicationWillResignActive` 内の expiration handler (line 59-66) は既にリセット済みなので**触らない**:

```swift
    var backgroundTaskID = UIBackgroundTaskIdentifier.invalid
```

```swift
    // アプリがアクティブになる度に呼ばれる
    func applicationDidBecomeActive(_ application: UIApplication) {
        // Issue #128: 初回 activation 等、background task 未登録なら何もしない。
        guard backgroundTaskID != .invalid else { return }
        application.endBackgroundTask(backgroundTaskID)
        backgroundTaskID = .invalid
    }
```

AppDelegate は UIKit lifecycle 直結で unit test 不能 (テストハーネス側の refactor が必要になり本スコープ超過) のため、テストは書かず既存 suite の green 維持で担保する。TDD 例外として PR 本文に明記すること。

- [ ] **Step 5: 全テスト green を確認**

```bash
cd /Users/shinya/workspace/claude/LeafTimer/app && make unit-tests
```

(Bash timeout 600000)。Expected: `** TEST SUCCEEDED **` あり、`** TEST FAILED **` なし。MockReviewRequester・ReviewIntegrationSpec・TimerCoreLogicSpec は無変更のまま通ること (通らない場合、mock に @MainActor を足す方向で直すのは**誤り**。原因を報告して STOP)。

- [ ] **Step 6: Commit**

```bash
cd /Users/shinya/workspace/claude/LeafTimer && git add app/LeafTimer/Components/StoreKitReviewRequester.swift app/LeafTimer/ViewModel/TimerViewModel.swift app/LeafTimer/App/AppDelegate.swift && git commit -m "fix(#128): ReviewRequesting の main thread 契約を @MainActor でコンパイル時検査に格上げ

- protocol の requestReview() に @MainActor を付与し、実装側の assumeIsolated を撤去
- 呼び出し側 TimerViewModel.requestReviewIfNeeded に境界 1 箇所の assumeIsolated を残す
  (VM 全体の actor 隔離は #69 スコープ)
- AppDelegate.backgroundTaskID: 初期値 .invalid 化 + didBecomeActive の guard + リセット追加"
```

---

### Task 2: #129 ViewInspector index パスの find() 化と xit 群の棚卸し

**Files:**
- Modify: `app/LeafTimerTests/ModernTimerViewSpec.swift`
- Modify: `app/LeafTimerTests/TimerViewSpec.swift`

**Interfaces:**
- Consumes: なし (Task 1 と独立。ただし Task 1 の後に実施し、常に green の suite の上で作業する)
- Produces: PR 本文に載せる**全 9 件の disposition 表** (復活 / 削除 / 理由)

**前提知識 (implementer は最初に `app/LeafTimer/View/TimerView.swift` を Read すること):** 現在の TimerView は `NavigationStack > GeometryReader > ZStack > ... VStack { Text(時刻) ... CircleButton ... 今日のカウント Text }` 構造で、`.navigationTitle` (line 125 付近) と `.toolbar` (line 127 付近) を持つ。xit の大半は「index パスが旧構造を指している」だけで、**検証対象の要素自体は現存する**。

**判定ルール:** 各テストを `find()` 形式 (`find(text:)` / `find(ViewType.View<CircleButton>.self)` / `find(ViewType.Toolbar.self)` 等) で書き直して通るなら**復活**。ViewInspector 0.10.2 の API 制約で要素に到達できないなら**削除** (xit のまま残さない。理由を disposition 表に記録)。判断に迷ったら xit のまま残して理由を表に書き、報告する。

**事前確定済みの disposition:**

| ファイル:行 | テスト | 確定方針 |
| --- | --- | --- |
| ModernTimerViewSpec:41 | has proper navigation title | **削除** — ViewInspector 0.10.2 の `navigationTitle()` は Binding 形式のみ対応 (Issue #16 記録済み)。find() では復活不可 |
| ModernTimerViewSpec:57 | displays timer with modern typography | find() で復活を試行 |
| ModernTimerViewSpec:82 | has CircleButton for timer control | find() で復活を試行 |
| ModernTimerViewSpec:99 | has reset button in toolbar | find() / `toolbar()` 到達を試行。ViewInspector の toolbar 探索は制約が多く、到達不能なら削除 |
| ModernTimerViewSpec:110 | has settings navigation link in toolbar | 同上 |
| ModernTimerViewSpec:119 / :131 | shows / updates today's session count | find(text:) で復活を試行 |
| ModernTimerViewSpec:146 付近 | displays GIF animation (Visual Feedback) | find(ViewType.View<GIFView>.self) で復活を試行 |
| TimerViewSpec:28 (xit "displayed remaining time.") | index パス | find(text:) で復活を試行 (期待値 "25:00" は UserDefaults 依存の可能性あり — fail するなら理由を調べて期待値を実態に合わせる) |
| TimerViewSpec:35 (xit "displayed start button." — 中身全コメントアウト) | 空殻 | **削除** — 検証内容が存在しない |
| TimerViewSpec:48 / :55 (toolbar / button tap) | index パス | find() で復活を試行。到達不能なら削除 |

live な index パスも対象: ModernTimerViewSpec 内の `it` で `zStack(0).vStack(1)` 系を使っているもの (例: timer control 周辺) があれば同時に find() へ寄せる。

- [ ] **Step 1: TimerView.swift を Read して現構造を把握する**

- [ ] **Step 2: 上の表の順に 1 テストずつ書き直し、都度そのテストだけ実行して GREEN/到達不能を判定する**

書き直し例 (session count):

```swift
it("shows today's session count") {
    let countText = try timerView.body.inspect()
        .find(text: String(timerViewModel.todaysCount))
    expect(countText) != nil
}
```

個別実行は `-only-testing` を使う:

```bash
cd /Users/shinya/workspace/claude/LeafTimer/app && xcodebuild -workspace 'LeafTimer.xcworkspace' -scheme "LeafTimer" -destination "platform=iOS Simulator,name=iPhone 17,OS=latest" test -only-testing:LeafTimerTests/ModernTimerViewSpec 2>&1 | tail -30
```

(Bash timeout 600000)

- [ ] **Step 3: disposition 表を完成させる (全 9 行、空欄なし)**

各行: 復活 (find() 形式) / 削除 (理由) / xit 維持 (理由)。この表は Task 4 の PR 本文にそのまま載せる。**silent drop 禁止 (ルール 19)。**

- [ ] **Step 4: 全テスト green を確認**

```bash
cd /Users/shinya/workspace/claude/LeafTimer/app && make unit-tests
```

(Bash timeout 600000)。Expected: `** TEST SUCCEEDED **`。

- [ ] **Step 5: Commit**

```bash
cd /Users/shinya/workspace/claude/LeafTimer && git add app/LeafTimerTests/ModernTimerViewSpec.swift app/LeafTimerTests/TimerViewSpec.swift && git commit -m "test(#129): ViewInspector の index ベースパスを find() 形式へ移行し xit 群を棚卸し

disposition 表は PR 本文を参照。#76 (UI テストターゲット整理) は本 PR のスコープ外で open のまま。"
```

---

### Task 3: #130 `make add-file` ターゲット化 (既存 add-to-target.rb のラップ)

**Files:**
- Modify: `app/bin/add-to-target.rb` (require の LoadError ガード追加のみ)
- Create: `app/bin/test_add_to_target.rb` (minitest、CLI をサブプロセス実行で検証)
- Modify: `app/Makefile` (`add-file` ターゲット追加)

**Interfaces:**
- Produces: `make add-file FILE=LeafTimer/Components/Foo.swift TARGET=app` / `make add-file FILE=LeafTimerTests/FooTests.swift TARGET=test`。TARGET は必須 (既定値なし)。実行後に自動で `make sort` + `make precheck` が走る。

**スコープ注記 (PR 本文と issue #130 コメントに必ず書く):** issue 原文は「pbxproj 手編集が毎回必要」という前提で純粋文字列変換スクリプトの新規作成を提案しているが、`app/bin/add-to-target.rb` (xcodeproj gem 使用、idempotent、Issue #3/#15 期から存在し過去 plan でも使用実績あり) が既にその中核を担っている。本タスクは**新規作成でなく既存スクリプトのラップ**とし、issue の残ギャップ (make ターゲット不在 / TARGET 取り違え防止 / 自動 sort / テスト不在) だけを埋める。

- [ ] **Step 1: minitest を書く (RED を先に確認)**

`app/bin/test_add_to_target.rb`:

```ruby
#!/usr/bin/env ruby
# add-to-target.rb の CLI 契約をサブプロセス実行で検証する。
# xcodeproj gem 必須のため make tests / precheck のチェーンには入れない (CLAUDE.md ルール 27)。
require "minitest/autorun"
require "tmpdir"
require "fileutils"
require "open3"

class AddToTargetTest < Minitest::Test
  BIN = File.expand_path("add-to-target.rb", __dir__)
  REAL_PROJ = File.expand_path("../LeafTimer.xcodeproj", __dir__)

  def with_tmp_proj
    Dir.mktmpdir do |dir|
      proj = File.join(dir, "LeafTimer.xcodeproj")
      FileUtils.cp_r(REAL_PROJ, proj)
      Dir.chdir(dir) { yield proj }
    end
  end

  def run_cli(*args)
    Open3.capture2e("ruby", BIN, *args)
  end

  def test_adds_new_file_to_app_target
    with_tmp_proj do |proj|
      out, status = run_cli(proj, "LeafTimer/Components/PlanFixtureDummy.swift", "LeafTimer", "LeafTimer/Components")
      assert status.success?, "expected success, got: #{out}"
      assert_includes out, "added:"
      assert_includes File.read(File.join(proj, "project.pbxproj")), "PlanFixtureDummy.swift"
    end
  end

  def test_rerun_is_idempotent_noop
    with_tmp_proj do |proj|
      _, first = run_cli(proj, "LeafTimer/Components/PlanFixtureDummy.swift", "LeafTimer", "LeafTimer/Components")
      assert first.success?
      out, second = run_cli(proj, "LeafTimer/Components/PlanFixtureDummy.swift", "LeafTimer", "LeafTimer/Components")
      assert second.success?
      assert_includes out, "no-op"
    end
  end

  def test_unknown_target_fails_red
    with_tmp_proj do |proj|
      out, status = run_cli(proj, "LeafTimer/Components/PlanFixtureDummy.swift", "NoSuchTarget", "LeafTimer/Components")
      refute status.success?, "expected failure for unknown target"
      assert_includes out, "target not found"
    end
  end

  def test_wrong_arity_fails_red
    out, status = run_cli("only-one-arg")
    refute status.success?
    assert_includes out, "usage:"
  end
end
```

- [ ] **Step 2: RED/GREEN の実行確認**

```bash
cd /Users/shinya/workspace/claude/LeafTimer/app && ruby bin/test_add_to_target.rb
```

Expected: 4 例とも **pass** (既存スクリプトが既に正しいため。`test_unknown_target_fails_red` と `test_wrong_arity_fails_red` が「壊した入力で RED になる」ことの実証 = ルール 8)。fail した場合は既存スクリプトの実挙動 (メッセージ文言等) に assert を合わせる — スクリプト側の仕様を勝手に変えない。

- [ ] **Step 3: add-to-target.rb の require を LoadError ガード化**

`require 'xcodeproj'` を以下に置換 (ルール 27):

```ruby
begin
  require "xcodeproj"
rescue LoadError
  abort "xcodeproj gem が必要です: gem install xcodeproj"
end
```

再度 `cd /Users/shinya/workspace/claude/LeafTimer/app && ruby bin/test_add_to_target.rb` で green を確認。

- [ ] **Step 4: Makefile に add-file ターゲットを追加**

`app/Makefile` の `sort:` ターゲットの直後に追加 (レシピ行の先頭は**タブ**):

```make
# 新規 Swift ファイルの pbxproj 配線 (Issue #130)。TARGET は取り違え事故防止のため必須。
#   make add-file FILE=LeafTimer/Components/Foo.swift TARGET=app
#   make add-file FILE=LeafTimerTests/FooTests.swift TARGET=test
add-file:
	@test -n "$(FILE)" && test -n "$(TARGET)" || { \
		echo "usage: make add-file FILE=<project-relative path> TARGET=app|test"; exit 1; }
	@test -f "$(FILE)" || { echo "error: file not found: $(FILE)"; exit 1; }
	@case "$(TARGET)" in \
		app)  XCTARGET=LeafTimer ;; \
		test) XCTARGET=LeafTimerTests ;; \
		*) echo "error: TARGET must be 'app' or 'test' (got '$(TARGET)')"; exit 1 ;; \
	esac; \
	ruby bin/test_add_to_target.rb && \
	ruby bin/add-to-target.rb LeafTimer.xcodeproj "$(FILE)" "$$XCTARGET" "$$(dirname "$(FILE)")"
	@$(MAKE) sort
	@$(MAKE) precheck
```

- [ ] **Step 5: make ターゲットの RED 3 経路 + GREEN 1 経路を実地確認**

```bash
cd /Users/shinya/workspace/claude/LeafTimer/app && make add-file; echo "exit=$?"
```

Expected: usage メッセージ + exit=非 0 (FILE/TARGET 未指定の RED)。

```bash
cd /Users/shinya/workspace/claude/LeafTimer/app && make add-file FILE=LeafTimer/Components/NoSuch.swift TARGET=app; echo "exit=$?"
```

Expected: `file not found` + 非 0。

```bash
cd /Users/shinya/workspace/claude/LeafTimer/app && touch /tmp/dummy-probe.swift && make add-file FILE=LeafTimer/Components/AppLogger.swift TARGET=both; echo "exit=$?"; rm /tmp/dummy-probe.swift
```

Expected: `TARGET must be 'app' or 'test'` + 非 0。

GREEN 経路は idempotent no-op で検証する (**既存登録済みファイル**を使うことで pbxproj を汚さない):

```bash
cd /Users/shinya/workspace/claude/LeafTimer/app && make add-file FILE=LeafTimer/Components/AppLogger.swift TARGET=app && git status --short
```

Expected: `no-op: ... already attached` が出て sort/precheck まで走り、`git status --short` で `project.pbxproj` に**差分が出ない** (idempotent の実証)。差分が出たら内容を確認し、sort による無害な並べ替えのみか判定して報告する。

- [ ] **Step 6: 全テスト green を確認**

```bash
cd /Users/shinya/workspace/claude/LeafTimer/app && make unit-tests
```

(Bash timeout 600000)。Expected: `** TEST SUCCEEDED **`。

- [ ] **Step 7: Commit**

```bash
cd /Users/shinya/workspace/claude/LeafTimer && git add app/Makefile app/bin/add-to-target.rb app/bin/test_add_to_target.rb && git commit -m "build(#130): 新規 Swift ファイルの pbxproj 配線を make add-file ターゲット化

- 既存 app/bin/add-to-target.rb (xcodeproj gem, idempotent) を Makefile でラップ
- TARGET=app|test を必須にして target 取り違えを防止、実行後に sort + precheck を自動実行
- minitest (test_add_to_target.rb) で unknown target / arity の RED を実証 (ルール 8)
- xcodeproj gem 不在時は actionable なメッセージで abort (ルール 27)。CI チェーンには不参加"
```

---

### Task 4: PR 作成 (merge はしない)

- [ ] **Step 1: 既存 PR の確認 (ルール 21)**

```bash
cd /Users/shinya/workspace/claude/LeafTimer && git fetch && gh pr list --state all --head feature/128-129-130-tech-debt-bundle
```

- [ ] **Step 2: push + PR 作成**

PR 本文に必ず含める: (1) 3 issue の対応サマリ、(2) #128 のスコープ判断 (protocol のみ @MainActor、VM 全体は #69。AppDelegate はテスト不能につき TDD 例外)、(3) #129 の disposition 表全 9 行 + 「#76 は open のまま」、(4) #130 の「issue 原文の前提は stale で、既存 add-to-target.rb のラップに変更」の注記。

```bash
cd /Users/shinya/workspace/claude/LeafTimer && git push -u origin feature/128-129-130-tech-debt-bundle && gh pr create --title "tech-debt: #128/#129/#130 バンドル (@MainActor 契約化 / ViewInspector find() 化 / make add-file)" --body "$(cat <<'EOF'
(上記 4 点を含む本文。Closes #128, Closes #129, Closes #130 を明記)

🤖 Generated with [Claude Code](https://claude.com/claude-code)
EOF
)"
```

- [ ] **Step 3: issue #130 に stale 前提の補足コメントを投稿**

```bash
gh issue comment 130 --body "PR 側の注記の通り、app/bin/add-to-target.rb が既に存在していたため (Issue #3/#15 期に導入済み)、本 issue は「新規スクリプト作成」でなく「既存スクリプトの make ラップ + テスト整備」として対応しました。"
```

---

## Self-Review 済み事項

- 3 issue の要求それぞれに対応タスクあり (#128→Task 1, #129→Task 2, #130→Task 3)。#128 の「併せて確認したい AppDelegate 小問題」も Task 1 Step 4 でカバー。
- #129 の issue が推奨していた「#76 と合わせて着手」は今回のバンドル構成 (ユーザー選択) によりスコープ外。PR 本文で明示する。
- 型整合: `ReviewRequesting` の変更は Task 1 内で完結し、他タスクは参照しない。
- プレースホルダなし: 全コード変更は実コードを記載済み。
