# Issue #138 / #77 / #76 Test-Infra Bundle Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** テスト基盤の負債 3 件 (#138 重複テスト削除 / #77 `DefaultTimerManager` と `LocalUserDefaultsWrapper` の実体テスト追加 / #76 形骸化した `LeafTimerUITests` ターゲットの削除) を 1 ブランチ 1 PR で完済する。

**Architecture:** Task 1 (#138) と Task 2 (#77) はテストターゲット中心 — 本番コードの変更は `LocalUserDefaultsWrapper` に `UserDefaults` 注入用の init (デフォルト `.standard`) を足す 1 点のみ。Task 3 (#76) は方針 (a) 「ターゲットごと削除」(ユーザー決定 2026-08-30) — pbxproj は手編集せず xcodeproj gem の one-off スクリプトで target を除去し、Podfile / xcscheme / precheck の `SOURCE_DIRS` から参照を落とす。Task 1+2 を 1 implementer、Task 3 を別 implementer に dispatch する (pbxproj 手術はレビュー面を分けたい)。

**Tech Stack:** Swift 5.9 / XCTest / Quick+Nimble+ViewInspector 0.10.2 (CocoaPods) / Ruby + xcodeproj gem 1.27.0 (ローカルに導入済み) / CocoaPods 1.16.2 / make

**Spec:** 本 plan が spec を兼ねる。要求の原文は GitHub Issue #138 / #77 / #76 本文 (`gh issue view <N>`)。

## Global Constraints

- ビルド/テストは毎回 `cd /Users/shinya/workspace/claude/LeafTimer/app && make unit-tests` 形式 (cd を同一コマンドに前置)。成否は exit code でなく `** TEST SUCCEEDED **` の存在 + `** TEST FAILED **` / `Error 6x` の不在で判定する (CLAUDE.md ルール 1)。`Mach error -308` / `Lost connection to testmanagerd` は Simulator 偽 FAIL — `xcrun simctl shutdown all && killall -9 com.apple.CoreSimulator.CoreSimulatorService` で再起動して 1 回リトライ。
- `make unit-tests` / `xcodebuild ... test` / `make tests` / `pod install` を含む Bash 呼び出しは timeout 600000 (10 分) を指定する (ルール 16)。
- 新規 Swift ファイルの配線は `make add-file FILE=<project相対パス> TARGET=test` を **1 ファイルずつ** 実行する (`&&` で 2 つ連結すると 1 回目の precheck が 2 つ目を orphan 判定して止まる — ルール 28)。pbxproj 差分をそのファイルを追加する commit に同梱する。
- 本番コードの変更は `app/LeafTimer/Components/LocalUserDefaultWrapper.swift` の init 追加のみ許可。それ以外の `app/LeafTimer/` は変更禁止。
- SwiftLint: 新規コードは `.isEmpty` を使う (ルール 35)。
- 各 Task 完了ごとに commit。メッセージは `test(#138): ...` / `test(#77): ...` / `build(#76): ...` 形式。
- **Task 3 の `git rm -r app/LeafTimerUITests` はユーザー自身が `!` で実行する** (ルール 14)。implementer は削除しない — Task 3 の brief に「ディレクトリは既に消えている」前提で入る。
- PR merge は plan のスコープ外 (ルール 22)。Task 4 の PR 作成で終了。
- Subagent は最終報告の全文を SendMessage で main へ送ってから idle になる (ルール 15)。
- RED 実証 (ルール 8) の対象は **新規に追加・強化した全テスト**。mutation を設計する前に「どの入力がその分岐を通るか」を確認する。

---

### Task 0: ブランチ作成 + plan commit (コントローラが実施)

- [ ] **Step 1: ブランチ作成と plan の first commit**

```bash
cd /Users/shinya/workspace/claude/LeafTimer && git checkout -b feature/138-77-76-test-infra-bundle && git add docs/superpowers/plans/2026-08-30-138-77-76-test-infra-bundle.md && git commit -m "docs(#138/#77/#76): テスト基盤バンドルの実装 plan を追加"
```

---

### Task 1: #138 — `Accessibility (Issue #59)` の被包含テストを削除

**Files:**
- Modify: `app/LeafTimerTests/ModernTimerViewSpec.swift:224-232`

**Interfaces:** なし

**前提:** `describe("Accessibility")` 内の `it("has accessibility labels for controls")` (196-203 行付近) は `find(ViewType.Button.self, where: { (try? $0.find(CircleButton.self)) != nil })` で同じ Button を見つけた上で `accessibilityLabel()` まで検証するため、`describe("Accessibility (Issue #59)")` の唯一のテストを厳密に包含する (PR #137 final review F-2)。

- [ ] **Step 1: 224-232 行の describe ブロックを丸ごと削除する**

削除対象 (この 9 行 + 直前の空行 1 行):

```swift

            describe("Accessibility (Issue #59)") {
                it("start/stop control is a real Button wrapping CircleButton") {
                    expect {
                        try timerView.body.inspect().find(ViewType.Button.self, where: { candidate in
                            (try? candidate.find(CircleButton.self)) != nil
                        })
                    }.toNot(throwError())
                }
            }
```

削除後、`describe("State Management")` ブロックの閉じ括弧の後に `}` (describe "Modernized TimerView") → `}` (spec) → `}` (class) が続く形になる。`grep -n "Issue #59" app/LeafTimerTests/ModernTimerViewSpec.swift` が 0 件になることを確認する。

- [ ] **Step 2: 包含側のテストが残っていて GREEN であることを確認する**

```bash
cd /Users/shinya/workspace/claude/LeafTimer/app && xcodebuild -workspace 'LeafTimer.xcworkspace' -scheme "LeafTimer" -destination "platform=iOS Simulator,name=iPhone 17,OS=latest" test -only-testing:LeafTimerTests/ModernTimerViewSpec 2>&1 | grep -E "Test Case .* (passed|failed)|error:|\*\* TEST (SUCCEEDED|FAILED) \*\*" | head -30
```

(timeout 600000)。Expected: `has accessibility labels for controls` が passed、`start/stop control is a real Button` が出力に**存在しない**、`** TEST SUCCEEDED **`。テスト数は 19 → 18。

- [ ] **Step 3: Commit**

```bash
cd /Users/shinya/workspace/claude/LeafTimer && git add app/LeafTimerTests/ModernTimerViewSpec.swift && git commit -m "test(#138): Accessibility (Issue #59) の被包含テストを削除

#133 で復活した \"has accessibility labels for controls\" が同一 finder + accessibilityLabel 検証で厳密に包含するため。"
```

---

### Task 2: #77 — `LocalUserDefaultsWrapper` / `DefaultTimerManager` の実体テスト

**Files:**
- Modify: `app/LeafTimer/Components/LocalUserDefaultWrapper.swift:11-12` (init 追加のみ)
- Create: `app/LeafTimerTests/LocalUserDefaultsWrapperTests.swift`
- Create: `app/LeafTimerTests/DefaultTimerManagerTests.swift`
- Modify: `app/LeafTimer.xcodeproj/project.pbxproj` (`make add-file` による自動配線 ×2、手編集禁止)

**Interfaces:**
- Produces: `LocalUserDefaultsWrapper.init(userDefaults: UserDefaults = .standard)` — 既存の `LocalUserDefaultsWrapper()` 呼び出し 5 箇所 (AppDelegate / TimerView / CircleButton / EnhancedSettingView) は無変更で `.standard` に解決される。
- Consumes: `TimerViewModel.init(timerManager:audioManager:userDefaultWrapper:sessionStatsRepository:)` と `TimerViewModel.updateTime()` (`endDate == nil` なら `currentTimeSecond -= 1`、`TimerViewModel.swift:154-176`)。test doubles は既存の `SpyAudioManager` / `MockUserDefaultWrapper` / `SpySessionStatsRepository` を使う。

- [ ] **Step 1: `LocalUserDefaultsWrapper` に注入用 init を足す**

`app/LeafTimer/Components/LocalUserDefaultWrapper.swift` の class 冒頭を以下に置き換える (残りは無変更):

```swift
class LocalUserDefaultsWrapper: UserDefaultsWrapper {
    private let userDefaults: UserDefaults

    /// Issue #77: テストから suite 指定の UserDefaults を注入できるようにする。
    /// 既存の呼び出し (引数なし) は .standard のまま。
    init(userDefaults: UserDefaults = .standard) {
        self.userDefaults = userDefaults
    }
```

- [ ] **Step 2: `LocalUserDefaultsWrapperTests.swift` を作成して配線する**

```swift
// app/LeafTimerTests/LocalUserDefaultsWrapperTests.swift
import XCTest
@testable import LeafTimer

/// Issue #77: 設定・統計の保存先そのものの実体テスト。Mock でなく suite 指定の UserDefaults に
/// 実際に書いて読む round-trip を検証する (パターンは SessionStatsMigrationTests を踏襲)。
final class LocalUserDefaultsWrapperTests: XCTestCase {

    private let suiteName = "LocalUserDefaultsWrapperTests"
    private var testDefaults: UserDefaults!
    private var wrapper: LocalUserDefaultsWrapper!

    override func setUp() {
        super.setUp()
        testDefaults = UserDefaults(suiteName: suiteName)
        testDefaults.removePersistentDomain(forName: suiteName)
        wrapper = LocalUserDefaultsWrapper(userDefaults: testDefaults)
    }

    override func tearDown() {
        testDefaults.removePersistentDomain(forName: suiteName)
        testDefaults = nil
        wrapper = nil
        super.tearDown()
    }

    func testIntRoundTrip() {
        wrapper.saveData(key: "workingTime", value: 3)
        XCTAssertEqual(wrapper.loadData(key: "workingTime") as Int, 3)
        // 実体の UserDefaults に書かれている (Mock でない) ことを直接確認
        XCTAssertEqual(testDefaults.integer(forKey: "workingTime"), 3)
    }

    func testIntOverwrite() {
        wrapper.saveData(key: "workingTime", value: 3)
        wrapper.saveData(key: "workingTime", value: 7)
        XCTAssertEqual(wrapper.loadData(key: "workingTime") as Int, 7)
    }

    func testIntMissingKeyReturnsZero() {
        XCTAssertEqual(wrapper.loadData(key: "never.saved") as Int, 0)
    }

    func testBoolRoundTrip() {
        wrapper.saveData(key: "vibration", value: true)
        XCTAssertTrue(wrapper.loadData(key: "vibration") as Bool)
        XCTAssertTrue(testDefaults.bool(forKey: "vibration"))
    }

    func testBoolMissingKeyReturnsFalse() {
        XCTAssertFalse(wrapper.loadData(key: "never.saved") as Bool)
    }

    func testKeysDoNotCollide() {
        wrapper.saveData(key: "a", value: 1)
        wrapper.saveData(key: "b", value: 2)
        XCTAssertEqual(wrapper.loadData(key: "a") as Int, 1)
        XCTAssertEqual(wrapper.loadData(key: "b") as Int, 2)
    }

    func testDefaultInitUsesStandardDefaults() {
        // 引数なし init は .standard を使う (既存 5 箇所の呼び出しの契約)。
        // .standard を汚さないよう固有キーを使い、テスト後に消す。
        let key = "LocalUserDefaultsWrapperTests.standard.probe"
        defer { UserDefaults.standard.removeObject(forKey: key) }
        LocalUserDefaultsWrapper().saveData(key: key, value: 42)
        XCTAssertEqual(UserDefaults.standard.integer(forKey: key), 42)
    }
}
```

配線 (1 ファイルずつ):

```bash
cd /Users/shinya/workspace/claude/LeafTimer/app && make add-file FILE=LeafTimerTests/LocalUserDefaultsWrapperTests.swift TARGET=test
```

- [ ] **Step 3: `DefaultTimerManagerTests.swift` を作成して配線する**

```swift
// app/LeafTimerTests/DefaultTimerManagerTests.swift
import XCTest
@testable import LeafTimer

/// Issue #77: タイマー発火の心臓部 DefaultTimerManager の実体テスト。
/// Timer.scheduledTimer は現在の RunLoop に載るため、XCTest の wait(for:) で RunLoop を回して
/// 実際に 1 秒刻みで target.updateTime() が呼ばれることを検証する。
final class DefaultTimerManagerTests: XCTestCase {

    private var timerManager: DefaultTimerManager!
    private var viewModel: TimerViewModel!

    override func setUp() {
        super.setUp()
        timerManager = DefaultTimerManager()
        viewModel = TimerViewModel(
            timerManager: timerManager,
            audioManager: SpyAudioManager(),
            userDefaultWrapper: MockUserDefaultWrapper(),
            sessionStatsRepository: SpySessionStatsRepository()
        )
        // endDate を設定しないので updateTime() は currentTimeSecond -= 1 の経路を通る
        // (TimerViewModel.swift:154-176)。0 に到達すると reset/通知経路に入るので十分大きい値にする。
        viewModel.currentTimeSecond = 100
    }

    override func tearDown() {
        timerManager.stop()
        timerManager = nil
        viewModel = nil
        super.tearDown()
    }

    func testStartFiresUpdateTimeEverySecond() {
        timerManager.start(target: viewModel)

        // 2 回発火 (≒ 2 秒) で 98 以下になる。tolerance 0.1 を見込んで 3.5 秒待つ。
        let decremented = XCTNSPredicateExpectation(
            predicate: NSPredicate { _, _ in self.viewModel.currentTimeSecond <= 98 },
            object: nil
        )
        wait(for: [decremented], timeout: 3.5)
        XCTAssertLessThanOrEqual(viewModel.currentTimeSecond, 98)
    }

    func testStopHaltsFiring() {
        timerManager.start(target: viewModel)
        let firedOnce = XCTNSPredicateExpectation(
            predicate: NSPredicate { _, _ in self.viewModel.currentTimeSecond <= 99 },
            object: nil
        )
        wait(for: [firedOnce], timeout: 2.5)

        timerManager.stop()
        let afterStop = viewModel.currentTimeSecond

        // stop 後に RunLoop を 2.5 秒回しても値が動かない
        RunLoop.current.run(until: Date(timeIntervalSinceNow: 2.5))
        XCTAssertEqual(viewModel.currentTimeSecond, afterStop)
    }

    func testStartTwiceDoesNotDoubleFire() {
        timerManager.start(target: viewModel)
        timerManager.start(target: viewModel) // 既存 timer を stop してから張り直す契約

        // 2.5 秒で発火は高々 2 回 (二重に張られていれば 4 回 = 96 まで落ちる)
        RunLoop.current.run(until: Date(timeIntervalSinceNow: 2.5))
        XCTAssertGreaterThanOrEqual(viewModel.currentTimeSecond, 97)
        XCTAssertLessThanOrEqual(viewModel.currentTimeSecond, 99)
    }
}
```

配線:

```bash
cd /Users/shinya/workspace/claude/LeafTimer/app && make add-file FILE=LeafTimerTests/DefaultTimerManagerTests.swift TARGET=test
```

- [ ] **Step 4: 新規 2 テストクラスを実行して GREEN を確認する**

```bash
cd /Users/shinya/workspace/claude/LeafTimer/app && xcodebuild -workspace 'LeafTimer.xcworkspace' -scheme "LeafTimer" -destination "platform=iOS Simulator,name=iPhone 17,OS=latest" test -only-testing:LeafTimerTests/LocalUserDefaultsWrapperTests -only-testing:LeafTimerTests/DefaultTimerManagerTests 2>&1 | grep -E "Test Case .* (passed|failed)|error:|\*\* TEST (SUCCEEDED|FAILED) \*\*" | head -30
```

(timeout 600000)。Expected: 10 件 passed、`** TEST SUCCEEDED **`。`DefaultTimerManagerTests` が timing で flaky なら、待ち時間を伸ばす前に **失敗時の実測値** (currentTimeSecond) を報告に書く。

- [ ] **Step 5: RED 実証 (commit しない一時変更、各 1 回ずつ、全て revert)**

| mutation (本番コードを一時的に壊す) | failed になるべきテスト |
| --- | --- |
| `LocalUserDefaultWrapper.swift` の `loadData(key:) -> Int` を `return 0` に固定 | `testIntRoundTrip` / `testIntOverwrite` / `testKeysDoNotCollide` (3 件)。`testIntMissingKeyReturnsZero` は pass のまま (分岐入力が 0 を期待するため) |
| `LocalUserDefaultWrapper.swift` の init 本文を `self.userDefaults = .standard` に固定 (注入を無視) | `testIntRoundTrip` の `testDefaults.integer(forKey:)` 検証と `testBoolRoundTrip` の同検証 (suite に書かれない) |
| `DefaultTimerManager.swift` の `strongTarget.updateTime()` 行をコメントアウト | `testStartFiresUpdateTimeEverySecond` / `testStopHaltsFiring` (wait timeout) |
| `DefaultTimerManager.swift` の `start` 冒頭の `stop()` 行をコメントアウト | `testStartTwiceDoesNotDoubleFire` (96 まで落ちる) |

各 mutation 後に Step 4 のコマンドを実行し、「壊した内容 → failed したテスト名」を報告に記録する。revert 後 `git diff app/LeafTimer/` が **init 追加のみ** であることを確認する。

- [ ] **Step 6: フル suite + precheck + lint**

```bash
cd /Users/shinya/workspace/claude/LeafTimer/app && make tests 2>&1 | grep -E "\*\* TEST (SUCCEEDED|FAILED) \*\*|Error 6|No rule to make target|orphan|error:" | head -20
```

(timeout 600000)。Expected: `** TEST SUCCEEDED **`、orphan 報告なし。

- [ ] **Step 7: Commit (pbxproj 同梱)**

```bash
cd /Users/shinya/workspace/claude/LeafTimer && git add app/LeafTimer/Components/LocalUserDefaultWrapper.swift app/LeafTimerTests/LocalUserDefaultsWrapperTests.swift app/LeafTimerTests/DefaultTimerManagerTests.swift app/LeafTimer.xcodeproj/project.pbxproj && git commit -m "test(#77): LocalUserDefaultsWrapper / DefaultTimerManager の実体テストを追加

- LocalUserDefaultsWrapper に UserDefaults 注入 init (default .standard) を追加し、suite 指定の round-trip を検証
- DefaultTimerManager は RunLoop を回して 1 秒刻みの updateTime 発火 / stop / 二重 start 防止を検証"
```

---

### Task 3: #76 (a) — `LeafTimerUITests` ターゲットの削除

**Files:**
- Delete: `app/LeafTimerUITests/` (Info.plist + LeafTimerUITests.swift) — **ユーザーが `! git rm -r app/LeafTimerUITests` で実施済みの前提** (コントローラが Task 3 dispatch 前に依頼する)
- Modify: `app/LeafTimer.xcodeproj/project.pbxproj` (one-off ruby スクリプト経由。手編集禁止)
- Modify: `app/LeafTimer.xcodeproj/xcshareddata/xcschemes/LeafTimer.xcscheme:41-50` (TestableReference 除去)
- Modify: `app/Podfile:18-22` (`target 'LeafTimerUITests'` ブロック除去)
- Modify: `app/bin/xcode-precheck.rb:41` (`SOURCE_DIRS` から `LeafTimerUITests` 除去)
- Create (一時、commit しない): `app/bin/remove-uitests-target.rb`

**Interfaces:** なし (Task 1/2 と独立)

**前提知識:** pbxproj 内の `LeafTimerUITests` 参照は 41 行 — PBXNativeTarget `3864B7E82494D82300948ACA` (buildPhases 6 つ、dependencies 1 つ、buildConfigurationList `3864B7F82494D82300948ACA`)、product `3864B7E92494D82300948ACA` (Products group)、source group `3864B7EC2494D82300948ACA`、Pods 由来の xcconfig 4 件 + framework 1 件 (Pods / Frameworks group)。xcodeproj gem の `remove_from_project` は referrer が 0 になった子 object を連鎖的に削除するので、target を消せば buildPhases / configList / dependency / containerProxy は自動で落ちる。group 経由で参照されている product / xcconfig / framework / .swift の file reference は明示的に消す。

- [ ] **Step 1: ユーザー削除済みを確認する**

```bash
cd /Users/shinya/workspace/claude/LeafTimer && git status --short && test ! -d app/LeafTimerUITests && echo "dir removed"
```

Expected: `D  app/LeafTimerUITests/Info.plist` / `D  app/LeafTimerUITests/LeafTimerUITests.swift` が staged、`dir removed`。無ければ **STOP して main に報告** (implementer は削除しない)。

- [ ] **Step 2: Podfile から UITests target を除去する**

`app/Podfile` の以下 5 行 (+ 直前の空行) を削除:

```ruby

  target 'LeafTimerUITests' do
    # Pods for testing
    pod 'Quick'
    pod 'Nimble'
  end
```

- [ ] **Step 3: one-off ruby スクリプトで pbxproj から target と参照を除去する**

`app/bin/remove-uitests-target.rb` を作成 (commit しない — Task 3 Step 9 で削除):

```ruby
#!/usr/bin/env ruby
# Issue #76: LeafTimerUITests ターゲットと関連参照を pbxproj から除去する one-off。
require "xcodeproj"

project = Xcodeproj::Project.open("LeafTimer.xcodeproj")
target = project.targets.find { |t| t.name == "LeafTimerUITests" }
abort "target not found (already removed?)" unless target

product = target.product_reference
target.remove_from_project          # buildPhases / configList / dependency は連鎖削除される
product&.remove_from_project

# source group (LeafTimerUITests/ 配下の file reference)
if (group = project.main_group.find_subpath("LeafTimerUITests", false))
  group.recursive_children.each(&:remove_from_project)
  group.remove_from_project
end

# Pods 由来の xcconfig / framework の file reference
project.files.select { |f| f.path.to_s.include?("LeafTimerUITests") }.each(&:remove_from_project)

project.save
puts "removed LeafTimerUITests target; remaining refs: #{File.read('LeafTimer.xcodeproj/project.pbxproj').scan('LeafTimerUITests').size}"
```

実行:

```bash
cd /Users/shinya/workspace/claude/LeafTimer/app && ruby bin/remove-uitests-target.rb
```

Expected: `remaining refs: 0`。0 でなければ `grep -n "LeafTimerUITests" LeafTimer.xcodeproj/project.pbxproj` の残りを報告に貼り、スクリプトを直して再実行する (pbxproj は手編集しない)。

- [ ] **Step 4: `pod install` で Pods 側の統合を更新し、pbxproj の安定性を確認する**

```bash
cd /Users/shinya/workspace/claude/LeafTimer/app && pod install 2>&1 | tail -5 && make sort && git -C /Users/shinya/workspace/claude/LeafTimer status --short
```

(timeout 600000)。次に **もう 1 回** `pod install && make sort` を実行し、2 回目で `project.pbxproj` / `Podfile.lock` に追加差分が出ないことを確認する (CI は `pod install` → `make tests` → `git diff --exit-code -- app/LeafTimer.xcodeproj/project.pbxproj` の順で走るため、committed pbxproj は pod install 後の安定状態でなければならない)。`Podfile.lock` は Quick/Nimble を LeafTimerTests が引き続き使うため無変更のはず — 差分が出たら内容を報告に貼る。

- [ ] **Step 5: xcscheme から TestableReference を除去する**

`app/LeafTimer.xcodeproj/xcshareddata/xcschemes/LeafTimer.xcscheme` の以下ブロック (41-50 行付近) を削除:

```xml
         <TestableReference
            skipped = "YES">
            <BuildableReference
               BuildableIdentifier = "primary"
               BlueprintIdentifier = "3864B7E82494D82300948ACA"
               BuildableName = "LeafTimerUITests.xctest"
               BlueprintName = "LeafTimerUITests"
               ReferencedContainer = "container:LeafTimer.xcodeproj">
            </BuildableReference>
         </TestableReference>
```

- [ ] **Step 6: precheck の `SOURCE_DIRS` を更新する**

`app/bin/xcode-precheck.rb:41` を `SOURCE_DIRS   = %w[LeafTimer LeafTimerTests].freeze` に変更する。

- [ ] **Step 7: 参照ゼロを確認する**

```bash
cd /Users/shinya/workspace/claude/LeafTimer && grep -rn "LeafTimerUITests" app --exclude-dir=Pods --exclude-dir=build --exclude-dir=DerivedData -l
```

Expected: 0 件 (`no matches found` でなく exit 1 で空)。`Podfile.lock` / `fastlane/` / `docs/` にヒットしたら報告に列挙する (docs は変更しない)。

- [ ] **Step 8: フル suite + precheck + lint**

```bash
cd /Users/shinya/workspace/claude/LeafTimer/app && make tests 2>&1 | grep -E "\*\* TEST (SUCCEEDED|FAILED) \*\*|Error 6|No rule to make target|orphan|error:|LeafTimerUITests" | head -20
```

(timeout 600000)。Expected: `** TEST SUCCEEDED **`、`LeafTimerUITests` の文字列が出力に現れない、precheck の `test_xcode_precheck.rb` も green。

- [ ] **Step 9: one-off スクリプトを消して commit**

```bash
cd /Users/shinya/workspace/claude/LeafTimer && rm app/bin/remove-uitests-target.rb && git add -A app/LeafTimerUITests app/LeafTimer.xcodeproj/project.pbxproj app/LeafTimer.xcodeproj/xcshareddata/xcschemes/LeafTimer.xcscheme app/Podfile app/bin/xcode-precheck.rb && git status --short && git commit -m "build(#76): 形骸化した LeafTimerUITests ターゲットを削除

testExample は app.launch() のみでアサーションゼロ、scheme でも skipped=YES で実行されていなかった。
方針 (a) ターゲットごと削除。pbxproj は xcodeproj gem で target/product/group/Pods 参照を除去し、
Podfile / xcscheme / precheck SOURCE_DIRS から参照を落とした。"
```

`git status --short` で `Podfile.lock` に差分があれば `git add app/Podfile.lock` も含める。commit 後に `git status --short` が空であることを確認する。

---

### Task 4: PR 作成 (merge はしない、コントローラが実施)

- [ ] **Step 1:** `git fetch && gh pr list --state all --head feature/138-77-76-test-infra-bundle` で既存 PR なしを確認して push。
- [ ] **Step 2:** PR 本文に Task 2 Step 5 の RED 実証表と Task 3 Step 4 の pod install 安定性確認結果を載せ、`Closes #138` / `Closes #77` / `Closes #76` を付ける。

---

## Self-Review 済み事項

- **Spec coverage:** #138 → Task 1。#77 (round-trip / クロージャ発火) → Task 2 (StoreKitReviewRequester は issue 本文通り対象外)。#76 (a) → Task 3。
- **Type consistency:** `LocalUserDefaultsWrapper(userDefaults:)` は Task 2 Step 1 で定義し Step 2 で使用。`loadData(key:) as Int` / `as Bool` のキャストは overload 解決のため必須 (`UserDefaultsWrapper` protocol に同名 2 メソッド)。
- **ルール 7 (実在確認):** xcodeproj gem 1.27.0 / pod 1.16.2 / `make add-file` / `SessionStatsMigrationTests` の suite パターン / `TimerViewModel.updateTime` の `endDate == nil` 分岐 / pbxproj の 41 参照 / xcscheme 41-50 行 / precheck.rb:41 — 全て本セッションで Read 済み。
- **既知リスク:** Task 2 の `DefaultTimerManagerTests` は実時間依存 (合計 ≒ 9 秒)。CI runner で flaky になったら待ち時間を伸ばすのでなく `XCTNSPredicateExpectation` の閾値を緩める方向で調整する。Task 3 の `pod install` が pbxproj を再整形する可能性 — Step 4 の 2 回実行で検出する。
